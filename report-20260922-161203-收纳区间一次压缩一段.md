# 交付报告 · 收纳的**内容切割**改成「一次压缩一段」

派单：#20260922-161203-收纳区间改成一次压缩一段（任务书 `手工派单-任务书-收纳区间改成一次压缩一段-20260922.md`）
cwd：`D:\apps\deepseek\dsh-memory-archive` ｜ 分支 `main` ｜ ⛔ 未 commit / 未 push / 未部署 / 未碰真机

改动四个文件（`git diff --stat`）：

```
 CHANGELOG.md               |  57 ++++
 _selftest-collect-scan.mjs | 657 ++++++++++++++++++++++++++++++++++++---------
 lib/collect-scan.js        | 526 ++++++++++++++++++++++--------------
 lib/index.js               |  39 ++-
 4 files changed, 957 insertions(+), 322 deletions(-)
```

一句话结论：**区间 = 一条压缩替换事件（它自己的 `shadowedSeqs`），摘要 = 那次压缩自己的模型摘要，
楼 = 只有真对话** —— 三件事一起改完，`lib/collect.js`（A1）、落库形状、HTTP 端点、放行门一字未动。

---

## 1 §2 逐条对照表（落在哪一行 / 怎么证的）

| §2 要求 | 落在哪儿（file:line） | 怎么证的（命令 + 真实输出） |
|---|---|---|
| **2.1** 分段判据 = 替换事件自带的 `shadowedSeqs`；一条替换 = 一个区间；⛔ 不再用 `seq === cur.toSeq+1`；⛔ 不排序后切相邻段 | `lib/collect-scan.js:184` `findShadowRegionsFromEvents()`（旧 `findShadowRegions` **已删**）；常量 `REPLACEMENT_TYPES` 在 `:76` | `node _selftest-collect-scan.mjs` → **1a/1b/1c/1d**：一条 `compaction/summary` ⇒ 恰好 1 个区间、区间 seq 集合逐字等于事件的 `shadowedSeqs`（含缺 seq 6）、乱序 `[5,1,3]` 原样保留（面顺序，⛔ 不排序）；**1e**：没有替换事件 ⇒ 0 区间 |
| **2.1** 注入原始事件（新 deps），⛔ 不改 `collectSurfaces()` 返回形状 | `lib/index.js:1361` `createRawEventLoader(ctx)`；注入点 `:3547` `/collect/scan`、`:3548` `/collect/auto`、`:5412` 自动收纳钩子；核内 `lib/collect-scan.js:1214` `loadRawEvents: (sid) => rawEventsViaHost(...)`；`collectSurfaces`（`:1988`）**一行未改**（仍是最近几楼 `:3845`+ 与未被遮蔽字数 `:3860`+ 的消费者） | 自检 **15b**（行为）：假宿主活注册表 ⇒ 装载器给出**带 `data`** 的原始事件（`via:'live'`）、`events[9].data.shadowedSeqs` 是数组；**15c**：同一条事件直接喂出 1 个区间、插件注入被挡在楼外；**15d**：没挂 `sessionQuery` ⇒ **抛**（不返回空数组冒充"没内容"）；**15e**：三条调用点接线锚 |
| **2.1** 分叉 `inheritedEventCount` 如实处理，⛔ 不重复收继承内容 | `lib/collect-scan.js:612`（`inheritedEventCount` 读出）+ `:627` warning；幂等仍只认台账 `contentHash` | 代码路径 + 自检 **5d–5h**（同一 plan 跑两遍 archived=0、台账哈希幂等）。⚠️ 真机分叉语义我量不到 ⇒ 见 §4「不确定」 |
| **2.2** 摘要 = 区间自己那条 `compaction/summary` 的 `data.summary` ⇒ `model-summary` | `lib/collect-scan.js:366` `normalizeReplacementEventDoc()`、`:426` `selectModelSummary(region)`；planScan 消费点 `:685` | 自检 **10a/10b/10c**（正文逐字 + meta + tags 收编）、**13e**（端到端：`summaryKind=model-summary`，正文 = 事件里那份）；退役的 `loadSummaryEventsViaHost` / `findCoveringSummaryEvents` / 旧 `normalizeSummaryEventDoc` 在**代码里零命中**，只剩注释在讲它们为什么退役（`:357`、`:1212`） |
| **2.2** 原因码枚举不变（`no-model-summary` / `ambiguous-coverage` / `model-summary-too-big` / `prompt-echo`）；prune / 空 / 超限 / 回声 ⇒ 机械兜底 | `selectModelSummary` 的四个 return（`:426`–`:452`）；prune 播报 `:708` | **10d**（同一段 seq 被两条替换声称 ⇒ mechanical + `ambiguous-coverage`）、**10e**（prune ⇒ mechanical + `no-model-summary` + 正文 = `buildRegionSummary` 逐字）、**10f**（空摘要）、**10g/10h**（tags）、**11a–11e**（回声闸门四种）、**13i**（prune 大声播报） |
| **2.2** ⛔ 机械兜底不再是常态 | 同上；无压缩事件 ⇒ 0 区间 | **10i2**：只有一条玩家消息的会话 ⇒ `scanned=0`、无 mechanical 条目（旧版会把它切成 N 个区间各回落一次） |
| **2.3** 排除 `tool/call` / `tool/result`：不成楼、⛔ 也不并进前一条楼 | `lib/collect-scan.js:77` `TOOL_TYPES`；排除点 `mapRegionToFloors` `:299` | **3a/3b**（tool 正文不并进前一条楼）、**4e**、**13o**（反证：挖掉那行 ⇒ tool 正文真的会并进楼 ⇒ 必红） |
| **2.3** `assistant/message` 正文只取文本块，取空 ⇒ 不成楼；⛔ 不用宿主那种把工具调用渲染成文本的口径 | `eventTextOf()` `lib/collect-scan.js:103`（只收 `{type:'text'}` 块；`data.message.content` / `data.content`） | **13c**（夹具里那条"全是工具调用/思维链块"的 assistant ⇒ 不成楼）、**14c**（真机形状夹具里 100+ 条 `memory_write` 式事件一条都没进楼）、**4c**（`memory_write` 式的空正文 ⇒ 不在楼里） |
| **2.3** 排除插件注入（`source.kind`）—— 守卫要**真的有牙** | `sourceKindOf()` `:127`（读 `data.source.kind`，这正是旧守卫恒 null 的那一环）、`isInjectedUserMessage()` `:147`、排除点 `:298` | **4c/4d**（`plugin` + `skill-catalog` 都不成楼、正文不并进）、**15b/15c**（换成原始事件后同一条判据生效）、**13n**（反证：挖掉 skip ⇒ 它的正文真的成楼 ⇒ 必红） |
| **2.3** 排除压缩的替换节点（紧跟 summary/prune 之后的 `user/message`） | `docOfEvent()` `:153` 的 `replacementNode`（判据 = 日志顺序上前一条事件的类型）、排除点 `:297` | **13l**（checkpoint 所在区间只剩两条真对话）、**13p**（反证：挖掉那行 ⇒ 英文 checkpoint 前言真的成楼 ⇒ 必红）、**5b**（三次压缩各自的 checkpoint 都不占楼号：楼号 `[6,7][8,9][10]`） |
| **2.3** 其余口径照旧（两类成楼、其余并入前楼、楼号从 `startFloor` 起） | `mapRegionToFloors` `:288` | **3e**（其余类型照旧并入）、**4a/4b**（楼号起点）、**4f**、**5b** |
| **2.4** `regionContentHash` 原料随口径变化，⛔ 不写"旧哈希也认" | `regionContentHash` `:261`（实现未改，口径与原料变了；头注 `:257-259` 写清）；`grep -i "compat\|legacy\|oldhash"` 在该文件只命中那句"⛔ 不写"的注释与既有的 `SCAN_COLLECT_INCOMPATIBLE` 错误码，**没有任何兼容分支** | **2a/2b/2c**（可复算 / 顺序敏感 / 等于口径直算值）；**5i**（台账仍是 64 位哈希键、只有计数/路径）；`git diff` 里没有任何兼容路径 |
| **2.5** 门 / plan-apply 语义 / 落库形状 / 端点表 / A1 一律不动 | `git diff --stat` 里**没有** `lib/collect.js`；`decideAutoCollect`、`publicRegionOf`、`statusOf`、台账 schema 未改 | **5j**（`planHash` 仍是 `scanplan-<16hex>`、无 `planId`）、**12e/12g**（两条端点仍 plan / plan+落库，状态码 200）、**7a/7c**（`too-big` / `empty` 原因码沿用），全量门 **67 个文件 0 失败**（含 A1 自己的台子） |

---

## 2 §3 六对自检的红 / 绿证据

命令一律：`node _selftest-collect-scan.mjs`（全台 **107 通过 / 0 失败**，退出码 0）。
反证**不是源码字符串断言**：拷一份 `lib` 到临时目录 → 只改/只挖那一处 → `import` 那份副本 → 喂**同一套夹具**。

| 对 | 相（新写法 = 绿） | 反证（旧写法跑一遍 = 必红） |
|---|---|---|
| ① 一次压缩一段 | `13b` 恰好 **1 个区间**；`13c` 楼里只有真对话（4 楼，工具/插件/思维链/`memory_write` 一个都不在） | `13d`：把分段判据改回「相邻即归一段」的副本跑同一夹具 ⇒ **2 个区间**（`region-0-5 / region-7-8`）⇒「恰好 1 个区间」必红。`1f` 用纯算术先算出这个对照数（2），`13d` 再验副本跑出来的一致 |
| ② 摘要来自那次压缩 | `13e` `summaryKind=model-summary`、正文逐字 = 事件 `data.summary` | `13f`：把输入换成便宜路径的**文档形状**（`{sessionId,seq,type,time,surface,text}`，无 `data`）⇒ 新代码 **0 区间**（连压缩都看不见）；`13g`：同一份文档形状喂旧判据 ⇒ 区间还在，但**每一条**都回落 `mechanical/no-model-summary` —— 这就是真机 §1-① 的 208/208 形态，把"为什么不能走那条便宜路"钉死 |
| ③ prune 区间 | `13i` prune ⇒ 仍出 1 个区间（合计 2 个），其摘要 `mechanical` + `no-model-summary` + warnings 点名 `compaction/prune`（⛔ 不静默） | 见 ②：旧写法下**所有**区间都退化成机械条目（prune 与 summary 分不开） |
| ④ 插件注入不成楼 | `13j`/`4c`/`4d`：`plugin` 与 `skill-catalog` 都不成楼、正文不并进前楼 | `13n`：副本里挖掉 `isInjectedUserMessage` 那行 ⇒ 楼里**真的**出现 `【占位】插件注入的…` ⇒ 断言必红 |
| ⑤ 全 tool 区间 | `13k` ⇒ `floorCount=0`、`skipped.reason=empty` + `COLLECT_REGION_EMPTY`（原因码沿用） | —— |
| ⑥ checkpoint 前言 | `13l`：紧跟 summary 的那条 `user/message` 所在区间只剩两条真对话，正文不含 `automatically generated checkpoint` | `13p`：副本里挖掉 `doc.replacementNode === true` 那行 ⇒ 英文前言**真的**成楼 ⇒ 断言必红 |

另：**13h** 锁死"反证全程没碰本仓的 lib"（原文件里两条判据都还在）；**13m/12h** 锁死"副本确实改到位"；
**13a/14f** 锁死副本用完即删。**13o** 是给 ①/③ 那条「工具正文不许并进楼」配的反证（挖掉 `TOOL_TYPES` 那行 ⇒
`工具调用占位串/工具结果占位串` 真的并进楼）—— 夹具里那 5 个楼正文的对照打印：

```
· ④/⑥ 反证：挖掉排除判据 ⇒ 楼 = ["甲甲甲甲甲甲甲甲甲甲","乙乙乙乙乙乙乙乙乙乙","【占位】插件注入的后","丁丁丁丁丁丁丁丁丁丁","己己己己己己己己己己"]
```

### 全量门

```
$ node "D:\apps\dsh-tarven配置区\产物\memory-tools\_run-all-selftests.mjs"      # exit=0
PASS node --check lib/index.js
PASS node --check lib/collect-scan.js
...
── 107 通过 / 0 失败 ──
  ✔ _selftest-collect-scan.mjs
=== 67 passed / 0 failed ===
```

（该脚本**只读只跑、一字未改**；A1 自己的台子 `_selftest-collect.mjs` 里那条
「index.js 只新增了 collect 分派行与端点表项」也照旧绿 —— 本单在 `lib/index.js` 上只加了
`createRawEventLoader` 与三处注入。）

---

## 3 真机形状夹具跑完整 `planScan`：改造前 / 改造后

夹具在自检台第 14 节：**按任务书 §1-① 的真机读数复刻结构**（**合成数据，不是真机会话**）——
一次 `compaction/summary` 的 `shadowedSeqs` 共 **279** 个、min **9** / max **1068**，其中被遮蔽的 seq 组成
**208** 段（宽度 138×1 + 69×2 + 1×3，段间的空隙是未被遮蔽的 slot）；节点构成也照真机比例
（工具事件、`plugin` 后处理指令、`skill-catalog` 技能清单、纯工具调用的 assistant 约占 1/5）。

**改造前**（同一份会话的**文档形状** + 旧判据的分段 —— 真机就是这条路的形态）：

```
· 改造前（旧判据 + 文档形状）：209 个区间（其中 208 段来自那一次压缩）；摘要来源 ["mechanical/no-model-summary"]；
  170 段有楼 / 其余 39 段被跳过
· 改造前前几个区间：region-9-9(楼1) / region-13-13(楼1) / region-17-17(楼1) / region-21-21(楼0)
```

⇒ **那一次压缩被切成 208 段**（+prune 段 1 个 = 209），`summaryKind` **100% `mechanical` / `no-model-summary`**
—— 与真机实测（208 区间 / 208 条 mechanical、147 条 80 字截断桩）同形。

**改造后**（新判据 + 原始事件）：

```
· 改造后（新判据 + 原始事件）：2 个区间 / {"model-summary":1,"mechanical:no-model-summary":1}；楼数 166+25
· 改造后区间：region-9-1068(model-summary) / region-1070-1100(mechanical)
```

⇒ **N = 压缩（含 prune）的次数 = 2**；其中 `region-9-1068` 是 **`model-summary`**（正文逐字 = 那次压缩的
`data.summary`，**不是** 80 字截断桩），`region-1070-1100` 是 **prune** ⇒ 如实机械兜底 + `no-model-summary`。
楼里再无工具面 / 插件注入 / checkpoint 前言 / 思维链（`14c` 逐条断言）。

---

## 4 没做 / 不确定（⛔ 请派单方核这些）

1. **真机上重收出来的东西对不对 —— 不是我的活，我一个字没下结论。** 我只在自检台的合成夹具上跑通了
   `planScan`（上面 §3），真机干跑 `/collect/scan` 要等你 `_sync-plugin-deploy.mjs --apply --prune` +
   **重启宿主**之后核（改的是宿主进程里的模块）。⛔ 我没碰 `C:\Users\w\.dsh`、没碰 `D:\apps\dsh-tarven`、
   没连宿主 `:3080`、没装进任何 profile、没 commit。
2. **本单不做（任务书已点名）**：玩家首条消息里 Tavern 拼进去的 OOC/规则/思维模式文本仍留在楼正文里
   —— 那是**玩家消息正文**，要切开得另立判据。
3. ⚠️ **区间变大之后 `maxFloorsPerRegion`（默认 200）头一次会真的咬人**：一次压缩落成的楼数若 > 200，
   该区间整段进 `skipped: too-big`（其余区间照常）。本夹具那一次压缩是 **166 楼**（未触线）；真机那条是
   **214 楼**（其中 44 条是机制楼，剔除机制面后按说在 200 以下 —— 但**我量不到真机**）。
   ⇒ **部署后干跑时请顺带看一眼 `skipped` 里有没有 `too-big`**；真咬到了，处置是升
   `scanConstants.maxFloorsPerRegion` 或请求体里的 `maxFloorsPerRegion`（本单没动它：它是既有阈值，
   改它属于"改 plan 语义"，超出本单范围）。
4. **分叉会话的 `inheritedEventCount` 语义**：我按"事件数组里含继承前缀、这个数是前缀长度"实现
   —— 继承内容**照收**（那也是本会话的剧情），重复内容靠台账 `contentHash` 幂等跳过；`>0` 时给一条
   warning 如实说明。但**真机 fork 会话我读不到**（§1 说明harness 读不到）：如果宿主返回的 `events`
   **不含**继承前缀，那么引用继承 seq 的区间会走 `missingDocCount`（我会**如实播报**
   "region X 有 N 个被遮蔽的 seq 在会话日志里找不到"，不会静默少收）。这一条请在真机上看一眼 warning。
5. **`compaction/prune` 的真机形态我按 §1-⑦ 的字段实现**（有 `shadowedSeqs`、无 `summary`）；真机上
   这次事故里没有 prune 事件，所以 prune 分支只有合成夹具的证据（`13i` / `14a` 的那一条）。
6. **`source.kind` 的取值集合**按 `lib/prompt-viewer.js` 的真机实测三种（`'user'` / `'plugin'` /
   `'skill-catalog'`）实现为"**不是 `'user'` 的字符串**都算注入"；kind **缺失**（老日志）⇒ 照旧当玩家消息
   （宁可留下可疑的，也不静默丢真发言）。若宿主以后新增一种"真玩家"来源，它会**多留**而不是少留。
7. **楼正文的抽取口径**：只取 `{type:'text'}` 块（`data.message.content` / `data.content`）。
   非消息类型（`system/message`、`request/header`、`turn/*` 等）仍照"其余类型并入前一条楼"的老规则处理，
   但它们**没有**这两处正文 ⇒ 实际并入的是空串 —— 这是**收紧**（用户的抱怨正是"原文更是全部都有"），
   但确实比旧版少收这些机制正文；如实记在这里。
8. **替换节点的判据是"日志顺序上的紧邻"**（官方契约原话 "That adjacency is contractual"，任务书 §1-⑦）。
   万一宿主在 `compaction/summary` 与它那条 checkpoint `user/message` 之间**插进了别的机制事件**，
   紧邻判据就失效、那条英文前言会落回楼里 —— 兜底是 `source.kind` 判据（日志里若带 `plugin` 来源会被
   ②挡掉），但我读不到真机日志、无法确认它带不带。⇒ **部署后干跑时值得扫一眼**：归档楼正文里若还有
   `automatically generated checkpoint` 那种英文前言，请告诉我，我把判据加宽（例如按
   "前面最近一条消息类事件是替换事件"判，而不是严格紧邻）。

---

## 5 CHANGELOG

`CHANGELOG.md` 的 `[Unreleased]` 顶部补了一条
「2026-09-22（收纳的内容切割：区间 = **一次压缩一段**、摘要 = **那次压缩自己的模型摘要**、楼只收真对话）」
（**⛔ 没有起新版本号**，`package.json` 仍是 `0.7.0`）。

---

## 6 复核用的命令（我跑过的原样）

```bash
node --check lib/collect-scan.js && node --check lib/index.js      # 都过
node _selftest-collect-scan.mjs                                    # 107 通过 / 0 失败（退出码 0）
node "D:\apps\dsh-tarven配置区\产物\memory-tools\_run-all-selftests.mjs"   # 67 passed / 0 failed
git diff --stat                                                    # 见文首（4 个文件，无 lib/collect.js）
```
