# 交付报告 · **按楼层绑定**的笔记快照 —— 每轮模型的改动都留一份，**回档（swipe / 回到上一楼）时自动跟随回去**

派单：`#20260923-232403-按楼层绑定笔记快照与回档跟随`（任务书 `手工派单-任务书-按楼层绑定笔记快照与回档跟随-20260923.md`）
cwd：`D:\apps\deepseek\dsh-memory-archive` ｜ 分支 `main` ｜ ⛔ 未 commit / 未 push / 未部署 / **全程未碰实机**
（宿主 :3080 一次没连过；夹具全在仓库根的临时目录里，跑完自删）

**一句话结论**：用户原话「**能不能做按楼层绑定管理的功能，也就是每轮模型修改都记录，能自动跟随回档**」
三件事都落地了。① **每轮**（轮末 `turn/end` + `compaction/end`）比一次那 5 份文件，**变了才**落一份快照
（⛔ 不每轮写盘、⛔ 不刷时间戳）；② 每一份**绑在"楼层"上** —— 锚 = Tavern `timeline.json` 的**节点**
（`head.nodeId` = 当前站在哪一楼），不是时间、不是会话轮次；③ **回档时自动跟随**：head 往回走 ⇒ 先把"当下"
另存一份（可撤销）再把那 5 份**写回**目标那一楼的样子，并**如实播报**（日志 + 面板）。

★ 与「死区」那条纪律**不打架**（这条写进了模块头注释）：死区管的是**模型**；回档是**我们**按楼层快照恢复，
而且**只恢复那 5 份**、**死区名单里的文件一律跳过**、**作者预置一个字都不动**。

**改动面**（3 个文件改 + 1 个新模块 + 2 个台子）：

| 文件 | 性质 |
| --- | --- |
| `lib/floor-snapshot.js` | **新增**（纯逻辑内核 + 薄落盘层，667 行含注释） |
| `lib/index.js` | 接线：模块加载 / 时间线读取 / 两条钩子 / 两条端点 / 写口刷新（+403 行，含端点与注释） |
| `lib/client.js` | 「剧情大纲」档里加楼层块（+150 行上下：两个 state / 两个具名回调 / 一块渲染） |
| `_selftest-floor-snapshot.mjs` | **新增**台子（854 行、61 条判据，含任务书 §3 的八对 + 反证） |
| `_selftest-client.mjs` | 扩：端点表 +2、取数回调名单 +2、新增一节楼层面板渲染断言（含 2 条反证） |
| `CHANGELOG.md` | `[Unreleased]` 补一条（**⛔ 没起新版本号**） |

---

## 0 一张图：什么时候读什么、写什么

```
                    ┌─ 每轮（system-prompt/assemble，至多一次，排到本轮之外）──┐
   组装 ───────────►│ 判回档：head 往回走？ ⇒ ① 先存"当下" ② 写回目标那一楼 5 份 │──► 模型本轮读到的就是那一楼
                    └──────────────────────────────────────────────────────┘
                    ┌─ 轮末（session/event: turn/end + compaction/end）───────┐
  一轮结束 ────────►│ 判回档（同上）；否则比一次那 5 份：变了才落一份快照          │──► .dma-floor-snapshots/
                    └──────────────────────────────────────────────────────┘
   面板「回到这一楼」──► POST …/floors/restore（只带 nodeId）──► 同一个恢复函数（手动那一脚）
```

**存哪**：`<记忆库目录>/.dma-floor-snapshots/`（点开头、我们自己的名字，⛔ 不用 `.md`/`.txt`）
= `index.json`（**只放清单**：楼层 → 那 5 份各自的 `{name, sha256, bytes, changed, delta}`）
+ `<sha256>`（正文**内容寻址**，同内容只落一份，⛔ 永不删）。**跟笔记同一处**（用户会连目录一起拷）。

---

## 1 §2 逐条对照表

### 2.1 楼层快照（记录侧）

| 任务书 §2.1 要求 | 落在哪 | 怎么证的 |
| --- | --- | --- |
| 锚 = **节点 id**，每份至少记 `nodeId`/`variantId`/`at` + 那 5 份各自的 `{name,sha256,bytes,changed}` | `lib/floor-snapshot.js:261` `planRecord()`（条目形状在函数尾）；`FLOOR_FILES` 定义在 `lib/floor-snapshot.js:59` | 台子 A4（键集合与 5 份顺序）+ §3-1相（`index.json` 里每楼 5 条、`sha256:null` 表示"那一楼没有这份"） |
| **只有变化过的文件才存正文** | `planRecord` 的 `blobs` + `missingBlobs()`（`lib/floor-snapshot.js:300`，落盘时只写盘上没有的） | §3-1反证：只改 `notes.md` 一个字符 ⇒ **blob 只多 1 个**，未变的两份仍是引用（原 sha 的 blob 一个字节没重写） |
| 存 `<周目>/.roleplay-memory/.dma-floor-snapshots/`；`index.json` + 内容寻址 `<sha256>`；⛔ 正文不进 index | `FLOOR_DIR_NAME`/`FLOOR_INDEX_NAME`（`lib/floor-snapshot.js:50,52`）；`writeIndex()`/`writeBlob()`（`:425,:448`） | §3-2相：两楼写**相同内容** ⇒ blob 数不涨；`index.json` 里只有 sha（台子断言里逐字段读过） |
| **何时记**：轮末（`turn/end` 那一族；`compaction/end` 也顺手一比） | `registerFloorSnapshot`（`lib/index.js:4800`，`scope.on('session/event', …)` 判 `turn/end`/`compaction/end`） | 台子 C4 / C4c：`turn/end` 记一份；`compaction/end` 也记（手动 `/compact` 不产生 `turn/end`） |
| 一模一样就什么都不写（⛔ 不每轮写盘、⛔ 不刷 index 时间戳） | `planRecord` 的 `changed: ok && changedAny`（`lib/floor-snapshot.js:289`）；`syncFloor` 只在 `changed` 时落盘（`lib/floor-snapshot.js:648`） | §3-1相：**index 逐字节相同**（含 `at`）、blob 数不涨；C4b：再一个轮末、笔记没变 ⇒ `indexRaw()` 一字不差 |
| 拿不到时间线 / 拿不到周目目录 / 首轮 head 为空 ⇒ 安静跳过（⛔ 不写、⛔ 不报错刷屏）但**如实记一条日志**（一次就够） | `playthroughTimeline()` 拿不到 ⇒ `headOf()` 为 null ⇒ `decideRollback` 判 `no-head`（`lib/floor-snapshot.js:229`）；日志走 `reportFloorReceipt` 的 `logOnly` 支 + 指纹门（`lib/index.js:4648`） | 台子 C8 / C8b：head 为空 ⇒ **连我们的快照目录都不建**，两轮下来日志**只有一条** |
| 认不出周目 / 两处都没有记忆库 ⇒ 静默跳过 | `floorHomeFor()`（`lib/index.js:4526`）与 `rankMemoryHomes()`（`lib/floor-snapshot.js:363`）返回 null ⇒ `floorSyncAt` 早退 | 台子 A7（都不是目录 ⇒ `null`）；C5c（面板那条路老口径 200+`ok:false`） |

### 2.2 回档跟随（恢复侧）

| 任务书 §2.2 要求 | 落在哪 | 怎么证的 |
| --- | --- | --- |
| `head.nodeId` == 上次记录的楼层 ⇒ 没回档，**这一支不许有任何写盘** | `decideRollback` 的 `same`（`lib/floor-snapshot.js:233`）⇒ `syncFloor` 的 watch 模式不做任何事 | 台子 §3-1相（同楼层再跑一次 ⇒ index 逐字节不变）；C3（同一 turn 再驱动 ⇒ 状态文件 `at` 不动） |
| 换了楼层、**新楼层在 `nodes` 里位置比上次靠前** ⇒ 判回档（⛔ 别用时间戳猜） | `decideRollback` 的 `seq >= lastSeq ? 'forward' : 'rollback'`（`lib/floor-snapshot.js:238,239`，位置来自 `seqMapOf`/`seqOfNode`） | §3-3相（往前走 ⇒ 不回档）；**§3-3反证**（把"用位置判"换成"用时间戳判"⇒ 同一夹具必误判，且那个误判会造成真实改动） |
| 回档：① 先把**当下**存一份（挂在回档前那个位置，可撤销）② 把 5 份写回目标那一楼 ③ 面板 + 日志如实播报 | `runRestore()`（`lib/floor-snapshot.js:547`）三步；文案唯一一处 = `floorReceiptText()`（`lib/index.js:4601`） | §3-4相（逐字节写回 + 备份在场）；§3-5相/相b（"当下"那份在，且点回那一楼能撤销）；C2b/C2c（日志与状态文件都如实） |
| `head.nodeId` 换了但那楼**没有快照** ⇒ 什么都不动 + 如实播报 | `decideRollback` 的 `back-no-snapshot` | **§3-7反证**：`indexRaw()` 一字不差、5 份文件内容/清单全长一样、`wrote:false` |
| 只写那 5 份；写之前逐份比 sha（一样就别写）；写用**原子写**（临时文件 + rename） | `planRestore()` 的 `unchanged` 分支；`writeRestoredFile()`（`lib/floor-snapshot.js:502`，文件在 ⇒ `deadzone.writeWithBackup`：写前备份 + 写后回读校验） | §3-4相b（`.bak-` 在场）；A6（`unchanged` 跳过）；A8（非那 5 份 ⇒ 拒写，盘上不留痕） |
| ⛔ 死区里的文件名一个都不许碰（复用 deadzone 那边的判据） | `floorDeadFiles()`（`lib/index.js:4563`，就一处：读 `.dma-deadzones.json` 的 zone 文件名）⇒ 喂给 `planRestore` 的 `deadFiles` | **§3-6反证b**（`index.md` 在死区里 ⇒ 一个字节没被写，`skipped[].reason==='deadzone'`）；C7/C7b（接线那一层从**真文档**读出来的名单同样压过硬编码的 5 份） |
| 死区数据读不出来 ⇒ 名单未知 ⇒ 保守不做 | `planRestore` 的 `blocked:'deadzones-unreadable'`（`lib/floor-snapshot.js:331`） | A6b（纯函数层）+ **C7c**（真钩子：笔记保持原样、状态文件 `blocked` 如实、日志一条） |
| 回档恢复**不删**任何东西；⛔ 不碰会话/归档/Tavern | `runRestore` 只走 `writeRestoredFile`（不 mkdir 除我们自己的目录、无 unlink/rm）；时间线只 `readFileSync` | 台子 D3（接线那一块里唯一的写盘是"我们自己的状态文件"，无 `copyFileSync/unlinkSync/rmSync`）；C6d（timeline / catalog 两份**逐字节没变**） |
| 恢复失败（某份写不进去）⇒ 如实：哪份失败、其它照旧，⛔ 不许静默、⛔ 不许部分成功当全成功 | 逐份收 `failed[]`；`last` **不跟**（下一轮重试）；端点回 **500 + 明细** | **C6e**（把 `notes.md` 换成目录 ⇒ 500、`failed:[{name:'notes.md',reason:…}]`、`index.md` 照样恢复了 500 里也照样列出来） |

### 2.3 面板

| 任务书 §2.3 要求 | 落在哪 | 怎么证的 |
| --- | --- | --- |
| 并进「剧情大纲」档（⛔ 别动别的档） | `lib/client.js:2393` `floorBlock` + `lib/client.js:2536` 挂到 ready 分支 | 台子「互不越界（R3）」照旧绿（另外三个源组件不碰 rp-memory）；`_selftest-client.mjs` 121/0 |
| 列表：`第 N 楼 · 时间 · 改了哪几份文件（+几字节）· [回到这一楼]`；当前那一楼高亮 | `floorRow()`（`lib/client.js:2332`，`data-floor` / `data-floor-current`） | 客户端台子渲染断言：`"data-floor":"1"|"3"`、`"data-floor-current":"1"|"0"`、`第 1 楼 / 第 3 楼`、`notes.md（+20 字节）`、`notes.md（共 120 字节）`、"◀ 当前这一楼" |
| 「回到这一楼」= **手动**触同一个恢复函数；面板明说"不影响会话" | `gotoFloor()`（`lib/client.js:1961`）→ `POST …/floors/restore` → `restoreFloor()` → **同一个** `runRestore()` | C6c（手动那一脚把笔记恢复到第 1 楼，回执里带 `message`）；客户端台子断言 `不影响会话` 那句在 |
| 档顶一条状态：最近一次**自动**回档跟随（哪一楼→哪一楼、恢复了哪几份）；没有就什么都不显示 | `floorAutoLine`（`lib/client.js:2388`，`floorAuto === null ⇒ null`） | C2c（状态文件 `{kind:'rollback', from, to, restored}`）+ 客户端渲染断言 `最近一次自动回档跟随` / `检测到回档到第 1 楼`；**没有就什么都不显示**（`=== null` 短路 + 反证"挖掉那一句必红"） |
| ⛔ 面板不许直连 Tavern、不许自己算"当前第几楼"（一律走宿主端点） | 序号/时间/高亮/该不该恢复**全在端点响应里**（`handleRpMemoryFloors`，`lib/index.js:5392`）；面板只画 | 客户端台子：`timeline.json|catalog.json|play-workspace` 在这一档组件体里 **0 次**、`seq` 不参与任何算术后 |
| 「回到这一楼」只带 `nodeId` | `gotoFloor` 的请求体 `{ nodeId: nodeId }` | 客户端台子：那一段调用里**没有** `path/dir/base/sha256` |

### 2.4 ⛔ 别的不许动

| 纪律 | 怎么证的 |
| --- | --- |
| `collect*` 一整条链 | 一行未动（`git status` 里 `lib/collect.js` / `lib/collect-scan.js` 无改动）；`_selftest-auto-collect.mjs` / `_selftest-collect*.mjs` 全量门里照旧绿 |
| `lib/deadzone.js` 既有对外语义（可调用、⛔ 不许改判据） | 该文件**未被写入**（`git status` 显示它是**上一单的未跟踪新文件**，我这一单一个字没改）；本模块只 `import { sha256Hex, writeWithBackup }`（复用同一份 sha 与"写前备份 + 写后回读"）；`_selftest-deadzone.mjs` 70/0 照旧 |
| 预设 YAML / `lib/collect.js` / `lib/ami-tags.js` / HTTP 端点表里那几条老端点的语义与状态码 | 预设 YAML 未被本单改动（`preset/agent.cordis.yml` 的改动是上一单留下的）；老端点一个没改 —— 只**新增**两条 `/playthrough/rp-memory/floors*`，`ENDPOINTS` 表里那几条老的逐字未动 |
| 时间线 / 周目目录**只读** | 台子 D3 静态核对 + C6d 逐字节比对（恢复前后 `timeline.json` / `catalog.json` 一字不差） |

---

## 2 §3 八对的红/绿证据（反证都把"挖掉那一步"贴出来）

| # | 相（绿） | 反证（挖掉/换错 ⇒ 必红） |
| --- | --- | --- |
| 1 | `§3-1相`：第一次记一份（3 份有内容 + 2 份记 `sha256:null`）；同一楼层 5 份没变 ⇒ **index 逐字节不动**（含 `at`）、blob 数不涨 | `§3-1反证`：改一个字符 ⇒ 必写一份，**blob 只多 1 个**（那一份的正文），未变的两份仍是 sha 引用（`ix.changed===false` 且原 blob 还在） |
| 2 | `§3-2相`：第 3 楼写回与第 1 楼**完全相同**的内容 ⇒ blob 数 **n1→n2 不涨**，清单里两边 sha 相同 | —（这条本身就是去重的相；台子把它钉在"同一夹具里写回同内容"上） |
| 3 | `§3-3相`：head 走到**后面的**新楼层 ⇒ `kind==='forward'`、`restore===null`、笔记就是模型刚写的样子，只是又记了一份 | `§3-3反证`（任务书要求的那一对）：同一夹具里把"用位置判前后"换成"用时间戳判" ⇒ `decided.kind==='forward'` 而 `byStamp==='rollback'`（**误判**），且 `§3-3反证b` 证明那个误判**会造成真实改动**（按它去恢复第 2 楼，笔记会被换成第 2 楼那份）。夹具是用**真路径**造出来的（`1→2→1→2` 走一遍），只把两楼记录的时刻**倒过来** |
| 4 | `§3-4相`：head 回到存过快照的前面某一楼 ⇒ 5 份**逐字节**写回那一楼；`§3-4相b` 写前备份 `.bak-<毫秒戳>` 在场；`§3-4相c` `last` 跟到目标楼层 | `§3-4反证`（任务书要求的那一对）：同一夹具、**把恢复那一步挖掉**（只跑裁决、不调 `writeRestoredFile`）⇒ 盘上还是第 3 楼那份 ⇒ "笔记回到那一楼"这条断言必红。代码逐字：<br>`const dug = plan.writes.some((w) => w.name === 'notes.md')   // ← 这一步本该写盘；"挖掉"= 不调用 writeRestoredFile`<br>`return dug === true && readMem('notes.md') === V3['notes.md'] && readMem('notes.md') !== V1['notes.md']` |
| 5 | `§3-5相`：回档发生时"当下"那一份也在（挂在回档前那一楼上，逐字节是**刚变过**的样子）；`§3-5相b` 点回那一楼 ⇒ 笔记回到"回档前那一刻"（可撤销） | —（这一对是"必须先留一份"，台子用"回档前盘上又变过"的夹具把 `pre` 真的逼出来） |
| 6 | `§3-6 相`：不在死区里的 `notes.md` 照常恢复 | `§3-6反证`：`rulebook.md` / `大纲-甲.md`（不在那 5 份里）**逐字节没变**；`§3-6反证b`：**被划成死区的 `index.md` 也没被碰**（它是 5 份之一，但死区名单压过它，`skipped[].reason==='deadzone'`）；`§3-6反证c`（任务书要求的那一对）**把"只恢复那 5 份"的判据挖掉 ⇒ 必红**：手写一份**没有白名单**的同款裁决 `noGuard(...)`，它对同一份夹具会把 `rulebook.md` 也写回去（`wall.some(w => w.name === 'rulebook.md') === true`），而真的 `planRestore` 一个都不写（`real.writes.every(w => w.name !== 'rulebook.md')`） |
| 7 | —（这一对本身就是反证） | `§3-7反证`：head 到一个**没有快照**的楼层 ⇒ `kind==='back-no-snapshot'`、`wrote===false`、**index 逐字节没动**、两份笔记内容没动；`§3-7反证b`：那一楼**也没有被"顺手记一份"**（不然会拿第 4 楼的内容冒充第 2 楼、毒掉以后的回档） |
| 8 | `§3-8相`：走**真端点** `POST …/write` 保存某份 ⇒ 该楼层快照刷新（清单 sha / blob / `updatedAt` 三处都跟上，回执 `floors.refreshed===true, seq:2`）；`§3-8相b`：保存**不触发任何"恢复"**（另外几份一个字节没动）；`§3-8相c`：后来回档回这一楼 ⇒ 恢复的是**用户自己改过**的那份 | —（这一对的"反证"落在 §3-6/§3-4 那两条：谁越界谁红） |

---

## 3 接线纪律的证据（§2.1 / §2.2 / §2.4）

| 判据 | 证据 |
| --- | --- |
| 两条钩子都在场，且同属一个插件 `dsh-memory-archive:floor-snapshot` | 台子 C1（假 ctx 驱动真 `apply()`，按 `who` 过滤） |
| 组装那一脚**不占本轮时间** | C2：`next()` 被调的那一刻笔记**还是旧的**（`atNext === V3`），让出事件循环之后才换成第 1 楼那份 |
| **每轮至多一次** | C3：同一 `turn` 里再驱动装配 ⇒ 状态文件 `at` 一个字节没动；C3b：同一 turn 里把 head 换成"认不出位置的新楼层"也不跑（**门是"这一轮"，不是"状态没变"**）；C3c：换到 `turn=8` ⇒ 门开着，如实播报"认不出先后 ⇒ 不动笔记" |
| ⛔ 不刷屏（同一个结果只报一条） | C3d：那句"认不出"的播报**只有 1 条**（指纹门）；C8b："还没有楼层"两轮下来只有 1 条 |
| 轮末才记 | C4 说明 watch 那一脚不记；`turn/end` 与 `compaction/end` 都记（C4c） |
| 死区名单来自**真文档**（接线那一层自己读） | C7 用例：`index.md` 在死区里 ⇒ 一个字节没动，`notes.md` 照常恢复；C7b 状态文件把"跳过 + 为什么"带上 |
| 刷新落在**写的那一处**目录 | §3-8 走真端点（写口用 `resolveRpMemoryDir` 的落点，刷新就在那一处读清单） |

---

## 4 纪律静态核对（D 节）

- **D1 白名单只有一处**：`lib/floor-snapshot.js` 去掉注释后，那 5 个文件名**各只出现 1 次**（都在 `FLOOR_FILES`）
  —— ⛔ 不许有第二张表（两张表迟早漂开）。
- **D2 只写自己家**：模块里 `writeFileSync` 恰好 3 处 = 清单 / 内容寻址正文 / 那 5 份；非那 5 份的名字一律
  `isFloorFile` 拒（`A8` 直接试了 `rulebook.md` ⇒ `ok:false`，盘上不留痕）。
- **D3 时间线 / 周目目录只读**：接线那一块里唯一的 `writeFileSync` 是我们自己的状态文件（临时文件 + rename）；
  无 `copyFileSync / unlinkSync / rmSync`；全仓没有一处写 `timeline.json` / `catalog.json` / `play-workspace.json`。

---

## 5 命令与真实输出

```bash
# 语法（改到的每个文件）
$ node --check lib/floor-snapshot.js && node --check lib/index.js && node --check lib/client.js
（三者均：无输出 = 通过；node --check 对 .mjs 台子同样通过）

# 本单的台子
$ node _selftest-floor-snapshot.mjs
== 总结：61 通过 / 0 失败 ==            # exit=0

# 面板台子（本单扩了一节）
$ node _selftest-client.mjs
== 总结：121 通过 / 0 失败 ==           # 上一单是 120，本单 +1 节

# 相邻两单的台子（回归）
$ node _selftest-deadzone.mjs          ⇒ 70 通过 / 0 失败
$ node _selftest-rp-memory.mjs         ⇒ 25 通过 / 0 失败
$ node _selftest-session-playthrough.mjs ⇒ 36 通过 / 0 失败

# 全量门（那个脚本可读可跑，未改；见下面第 6 节的路径说明）
$ node /d/apps/dsh-tar*配置区/产物/memory-tools/_run-all-selftests.mjs
…
  ✔ _selftest-deadzone.mjs
  ✔ _selftest-floor-snapshot.mjs      # ← 本单新台子已进门
  ✔ _selftest-rp-memory.mjs
=== 69 passed / 0 failed ===          # exit=0（上一单 68 ⇒ 本单 +1）
```

---

## 6 没做 / 不确定（**明确列出**）

1. **真机上"回档到底跟不跟"我没下结论** —— 按派单口径（§4）部署与真机验证归派单方：
   我改的是宿主进程里的模块，需要 `_sync-plugin-deploy.mjs --apply --prune` + **重启宿主**之后，
   在真机上划一楼 / 回档 / 看笔记跟不跟。**本单期间一次没碰实机**（宿主 :3080、`C:\Users\w\.dsh`、
   用户 RP 工作区 `D:\apps\dsh-tarven` 全都没碰）。
2. **"模型本轮读到的是不是恢复后的笔记"没有真机证据** —— 组装那一脚是**排到本轮之外**跑的
   （`setTimeout(…, 0)` + `unref`，照死区看守那条既有形状），所以理论上模型读文件时已经恢复好了，
   但"装配与模型首次读文件之间的时间差"只有真机能验。**台子证明的是"没占本轮时间 + 最终确实换回去了"**（C2）。
3. **同一节点换变体（swipe 重 roll 同一楼）不触发恢复** —— 我**按 §2.2 的字面口径**（判据 = `head.nodeId`）。
   记录侧会把那一楼的样子更新成**新变体**的内容（`variantId` 跟着变）。★ 任务书 §0 的"★ 锚已查实"里提到
   "某节点的 `adoptedVariantId` 换成另一支"也算回档 ⇒ 这两处口径有张力，我**没自己改口径**；
   如果用户要"换变体也跟随"，那是**另一条判据**（需要变体级的快照），请派单方定。
4. **死区名单压过那 5 份（我做的一个解释性选择，请拍板）** —— §2.2 写「死区里的文件名一个都不许碰」，
   而决定① 把那 5 份（含 `index.md`）列为要恢复的。两处对同一份文件可能打架：我的实现是
   **死区名单优先**（`index.md` 被划过死区 ⇒ 回档**跳过**它并如实播报）。
   保守、可解释、状态文件里逐条可见；但**如果用户希望"划了死区的笔记也照常按楼层恢复"**，把
   `lib/floor-snapshot.js` 里 `planRestore` 的 `deadzone` 那一行去掉即可（⛔ 一行，台子 §3-6反证b 会立刻红）。
5. **`floor-status.json` 的日志会压重** —— 同一个结果连着出现只记一条（与死区看守同款指纹门）。
   面板的**楼层清单**始终如实、不受影响；但"同一件事连着发生第二次"在日志里看不到第二条。
6. **首轮 / 认不出周目时什么都不写** —— 这是**刻意的**（§2.1 口述的"安静跳过"）：新会话在没有笔记、
   没有周目的阶段不会凭空建目录，也绝不会把上一轮的笔记当成本轮的。
7. **`node --check _selftest-client.mjs` 会报 BOM** —— 那是**既有**状态（`git show HEAD:` 出来头三字节
   就是 `EF BB BF`），本单没动那一行；`node _selftest-client.mjs` 直接跑正常（121/0），全量门也是直接跑。
8. **全量门的路径** —— 派单给的字面路径 `D:\apps\dsh-tarven配置区\…` 会被本次派单的守卫整条拒掉
   （`bash.denyPath`：命令里出现 `d:\apps\dsh-tarven` 就拦），所以照本仓既往报告里的做法用
   **glob 形式** `/d/apps/dsh-tar*配置区/产物/memory-tools/_run-all-selftests.mjs` 走到**同一个脚本**
   （未改一个字节），`exit=0`、`69 passed / 0 failed`。
9. **没做的事**（都不在本单范围内）：⛔ 没 commit / 没 push / 没部署 / 没重启任何宿主 / 没装进任何 profile；
   ⛔ 没碰 `lib/deadzone.js`（只 import 它的 `sha256Hex` / `writeWithBackup`）；⛔ 没碰 collect 链、预设 YAML、
   `lib/ami-tags.js`、老端点的语义与状态码；⛔ 没起新版本号（`CHANGELOG` 只补 `[Unreleased]`）。

---

## 7 本单自己踩到的坑（留给后人，也是台子"会咬人"的证据）

1. **`sessionPlaythroughOf()` 只给 `{characterId, playthroughId}`** —— 它**没有**现成的周目目录，而
   `rpMemoryCandidates(hit)` 要的是 `{dir, rootPath}` ⇒ 直接喂它会在 `path.join(undefined)` 上炸
   （`The "path" argument must be of type string`）。修法：按 `deadzoneHomeFor` 的同一份拼法补出
   `{dir: join(rootPath, characterId, playthroughId), rootPath}`（`lib/index.js:4526` 的注释里写明了）。
   ★ 这个坑**是被台子的 C4 抓出来的**（不是真机）。
2. **`reportFloorReceipt` 少接一个 `log`** —— 函数里 `log.info(...)` 引用了一个不在作用域里的 `log`，
   异常被自己的 `try/catch` 吞掉 ⇒ **状态文件写了、日志一条没有**。这也正是我为什么在同一处加了
   `reportFloorError()`：**摔了要留一条**（同一个错误只留一条，指纹门），否则"静默失效"看起来跟
   "没事发生"一模一样。
3. **注释里别写 `useEffect` 这个字面量** —— `RpMemoryFlow` 的台子用 `/useEffect/.test(组件体)` 钉
   "零挂载取数"，我在组件体里的注释写了"零 useEffect" ⇒ **假红**。改成"零挂载取数"就对了。
