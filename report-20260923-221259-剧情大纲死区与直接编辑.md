# 交付报告 · 「剧情大纲」档加 **剧情文本死区（模型禁改区）** + **剧情文本直接编辑**

派单：`#20260923-221259-剧情大纲死区与直接编辑`（任务书 `手工派单-任务书-剧情大纲死区与直接编辑-20260923.md`）
cwd：`D:\apps\deepseek\dsh-memory-archive` ｜ 分支 `main` ｜ ⛔ 未 commit / 未 push / 未部署 / **全程未碰实机**

**一句话结论**：用户原话「**给剧情大纲加 1、剧情文本支持设置死区 2、剧情文本支持直接编辑**」两件事都落地了。
① **死区**＝模型禁改区：用户在某一份剧情文本里**按块**（markdown 块，空行分隔）划出"碰不得"的段落，
数据落**与笔记同一处的** `.roleplay-memory/.dma-deadzones.json`（跟着周目/目录走），约束**每轮**挂在
`mt:memoryHome` 段里喂给模型；**每轮至多比对一次**，不一致就**大声告警**（面板红字 + 日志），
★★ **⛔ 绝不自动还原**（用户明确否掉的那个选项 —— 本单没有任何一处会把快照写回文件）。
② **直接编辑**：过了手动确认之后，每个 `.md`/`.txt` 可就地摊开 textarea 改**整份正文**，保存走
**写前备份 + 白名单 + 乐观锁（409）**，保存成功**顺手刷新该文件的死区快照**（用户自己改的 ⇒ 快照跟着走，
**不误报**）。这一档此前整条链只读，**本单第一次给它开写**。

本轮我改了 3 个文件 + 新增 2 个（`git diff --numstat` 与新增文件行数）：

| 文件 | 改了什么 | +/− |
|---|---|---|
| `lib/deadzone.js` **（新）** | 死区/编辑的纯逻辑内核：块切法、快照比对与定位、注入那句、快照刷新、落盘裁决、写前备份+回读校验、文档读写 | 476 行（新） |
| `lib/index.js` | 三条新端点（write / deadzones / deadzones-status）+ `handleRpMemory` 补 `sha256`/`blocks`/`writable`/死区计数 + **死区看守**（每轮至多一次）+ `mt:memoryHome` 追加约束那句 | 507 / 3 |
| `lib/client.js` | `RpMemoryFlow`：逐块渲染 + 每块锁标、「✎ 直接改」编辑器、死区红字/一致绿字；**仍然零 useEffect** | 289 / 7 |
| `_selftest-deadzone.mjs` **（新）** | 宿主半侧台子：A 纯逻辑 10 条 · B **任务书 §3 八对**（每对带反证）· C 接线纪律 · D 面板契约 · E 真机形状 | 639 行（新） |
| `_selftest-client.mjs` | `ENDPOINTS` 表补三条；`rpMemGateContract` 扩成"五个具名回调之外零取数"；读端点计数收窄；**新增一节渲染断言** | 160 / 4 |

> 上一单的 `report.md` **改名保留**为 `report-20260922-175828-删除立刻执行.md`（⛔ 没删，任务书 §6）；
> 本单报告写在 `report.md`。工作区里没有别的未提交改动（`git status` 只有上面这 5 个文件 + 那次改名）。

---

## 1 逐条对照表（任务书 §2 ⇒ 落在哪 ⇒ 怎么证的）

### §2.1 死区（模型禁改区）

| # | 要求 | 落在哪 | 怎么证的（命令 + 真实输出） |
|---|---|---|---|
| ① | 数据落在**周目目录**里：`.roleplay-memory/.dma-deadzones.json`，点开头的我们自己的文件名 | 文件名 `lib/deadzone.js:47`；落点 = 「笔记所在那一处目录」= `lib/index.js:4798/4803`（`readDocFile/writeDocFile(found.dir)`，`found` 来自与读侧同一份候选链 `resolveRpMemoryDir()`） | `node _selftest-deadzone.mjs` 的 **D5**：`dz.DEADZONE_FILE_NAME === '.dma-deadzones.json'`、盘上就是它、且**清单里没有它**；E1/E4 证同一件事在**共用那份**落点上也成立。真 HTTP 旁证：清单响应里 `"skipped":1`（那一项就是它，被裁决挡在清单外） |
| ② | 每条死区 = 文件 + 原文快照 + 快照 sha256 + 可选备注 | 形状 `lib/deadzone.js:150-172`（`normalizeZone`）；写入在 `decideToggle:175` | **A4/A9**（文档自愈：手改过的 doc 以 `text` 为准重算 sha）+ **A10**（认不出的条目一律丢弃）；真 HTTP：`{"ok":true,…,"zone":{"file":"index.md","label":"核心规则","firstLine":"## 【必须遵守的核心规则】","sha256":"188f08a3…","chars":34},"count":1}` |
| ③ | 粒度 = **markdown 块**（空行分隔），⛔ 不做任意行范围 | `lib/deadzone.js:98`（`splitBlocks`）+ 文件头 `:44-50`（**为什么**：行号会漂移，块级快照同时解决定位与检测） | **A1**：`'\n\n## A\n- 1\n- 2\n\n## B\n'` 切成 2 块、块内换行保留、首尾空行不参与、**CRLF 与 LF 的块 sha 完全相同** |
| ④ | 每块一个「🔒 设为死区 / 解锁」；已死的块**灰底 + 锁标**；写操作走**新宿主端点**（⛔ 客户端不直接写文件） | 客户端 `lib/client.js:2109`（`blockRow`）/`:2125`（按钮 id `dma-rpmem-zone-<文件>-<块号>`）/`:2074`（`toggleZone` → `apiPost`）；宿主 `lib/index.js:4843`（`handleRpMemoryDeadzones`） | 客户端台子新增那一节：渲染树里有 `dma-rpmem-zone-index.md-0` / `-1`、`"data-dead":"1"` 与 `"data-dead":"0"`、文案 `🔒 死区 · 模型不许改` / `🔓 解锁` / `🔒 设为死区`；源码断言 `saveFile`/`toggleZone` 之外**零取数**，且 `toggleZone` 走 `apiPost` 而不是任何本地写 |
| ⑤ | 喂给模型：`mt:memoryHome` 追加，**逐字可读**、含**文件名 + 该段首行原文**，⛔ 原文整段不进注入 | `lib/index.js:4211`（段尾插 `deadzoneHintFor(sid)`）+ `:4276`（`deadzoneHintFor`）+ `lib/deadzone.js:301`（`renderHint`） | **B4b**（段里有文件名 + 首行 + 「你只有追加权」）、**B4c**（`- 不许替玩家做决定` 这类**原文**一个字都没进注入）、**B4e ★反证**（把死区全解除 ⇒ 那行不出现）。**真实输出**（`node _tmp-show-deadzone-hint.mjs`，取证后已删）：见下面 §3 第 4 对 |
| ⑥ | 检测：**每轮至多一次**、**不阻塞轮次**、**不许每轮重写状态文件** | `lib/index.js:4379`（`registerDeadzoneWatch`：门 = `turnBoundary.lastTurn`，`perTurn` LRU 64）+ `:4413`（`setTimeout(…,0)` + `unref` 排到本轮之外）+ `:4349`（**结果指纹**闸门） | **C 节 10 条**：C1（`next()` 那一刻还没告警，让出事件循环之后才有 ⇒ 真的不占本轮时间）、C2/C2b（同一 turn 再驱动两次 / 换一轮但结果没变 ⇒ 都不再比对）、**C4 ★**（后两轮连状态文件的 `at` 都没动）、C5（结构性：看门那条路上**没有 await**） |
| ⑦ | 不一致 ⇒ ① 面板红字 ② 日志一条 ③（可选）顶部状态 | 面板 `lib/client.js:2326`（`data-dma="deadzone-alert"`）；日志 `lib/index.js:4364`；状态文件 `lib/index.js:4308`（`writeDeadzoneStatus`），面板读它的 `lastWatch` | **B1c ★反证**（改一个字符 ⇒ `status="changed"`、指得出 `file/firstLine`、`snapSha` 与 `nowSha` 两个都在）、**C1b/C3**（日志真的记了）；顶部那条是**可选**项，**本单没做**（见 §4） |
| ⑧ | ⚠️ 文案**不许定性**（不写"模型违规"） | `lib/index.js:4364`（日志那条带「**可能是模型改的，也可能是你在别处改过**」）；客户端红字同句 | **C3b**（那条 warn 含两种可能 + 「不会替你改回去」）、**B1d**（状态端点只给事实）；客户端台子断言渲染文本里有这句原话 |
| ⑨ | ★★ 面板编辑保存成功 ⇒ **同步刷新该文件的死区快照**（不误报） | `lib/index.js:4797-4812`（`refreshAfterEdit` + `writeDocFile`）+ `lib/deadzone.js:268` | **B3b ★★**（保存后状态回 `ok`、**不告警**）、**B3d ★反证**（同一份新文、但绕过端点直接改盘 = "抽掉刷新那一步" ⇒ 必告警）、**B3e**（结构性：那条路上必须有 `refreshAfterEdit`） |
| ⑩ | ⛔ **不许自动还原**（被否掉的选项） | 全单没有任何"把快照写回文件"的调用：`compareZones`（`lib/deadzone.js:237`）与 `handleRpMemoryDeadzoneStatus`（`lib/index.js:4904`）里**零写盘** | **B8/B8b/B8c/B8d 四条**（见 §3 第 8 对） |

### §2.2 直接编辑（剧情文本可写）

| # | 要求 | 落在哪 | 怎么证的 |
|---|---|---|---|
| ① | **写前备份** `.bak-<时间戳>`（复制留档，⛔ 绝不销毁）+ 写后回读校验 | `lib/deadzone.js:444`（`writeWithBackup`：`copyFileSync` → `writeFileSync` → 回读逐字节比） | **B5c**（盘上真有那份备份，内容是**改前**那一份）、**B5f**（结构性：备份必须在写盘**之前**）、真 HTTP 回执 `"backup":"index.md.bak-2026-09-23T14-25-56-116"` |
| ② | **白名单**：`.roleplay-memory/` 下的 `*.md`/`*.txt`；`rel-path-jail.js` 只调用不修改；⛔ 拒穿越 / 拒别的扩展名 / ⛔ **拒不存在的文件** | 名字裁决复用 `rpMemoryNameOk`（`lib/index.js:4774`，内部就是 `relPathJail.validateRelPath`）；扩展名收窄在 `lib/deadzone.js:63` + `:356`（`decideWrite`） | **B6 ★反证**（`../../x.md`、`..%2Fx.md`、`C:\secret.md`、`/etc/passwd`、`a.exe`、`notes.json`、`CON.md`、`.hidden.md`、`sub/x.md`、空名、null ⇒ 一律拒，且**目录外那份一个字节没动**）、**B6b**（盘上没变、没多文件）、**B6c**（读侧同口径）；`git diff` 里 `lib/rel-path-jail.js` **一行都没改** |
| ③ | **乐观锁**：带用户看到的那份 sha256，落盘前重读比对；不一致 ⇒ **409 + 可读文案**；⛔ 静默覆盖 | `lib/deadzone.js:387`（`if (now !== want)`）；端点回 `plan.status`（`lib/index.js:4790`） | **B7/B7b**（409 + 文案含「先刷新再改」+ 盘上一个字节没变）、**B7c ★反证**（抽掉锁 = 直接覆盖 ⇒ "盘上没变"必红）、**B7d**（判据只认重算 sha，⛔ 不认 mtime）。真 HTTP：`HTTP 409 {"ok":false,"error":{"code":"RP_MEMORY_WRITE_STALE","message":"盘上已经变了（可能是模型刚写过，也可能是你在别处改过），先刷新再改 —— 你手里的那份没有覆盖上去"}}` |
| ④ | 保存成功 ⇒ 刷新该文件死区快照 | 同 §2.1 ⑨ | 真 HTTP：`"deadzones":{"refreshed":1,"updated":1,"dropped":[],"total":1,"error":null}`，紧接着 `status` 从 `changed` **回到 `ok`** |
| ⑤ | 回执**如实**：字节数 / sha256 / 备份名 / 刷新几条；⛔ 响应里不回正文 | `lib/index.js:4817-4827` | **B5**（回执四件套齐 + 响应体里**没有**新正文）；**B5d**（备份名不进清单）。真 HTTP：`{"ok":true,"file":"index.md","chars":67,"bytes":165,"sha256":"c8b0543c…","backup":"index.md.bak-…","deadzones":{…}}` |
| ⑥ | ⛔ 编辑内容不塞进任何 system 段 | 全单只改文件；`mt:memoryHome` 那套一个字没动（只**追加**了死区那句） | 客户端台子：`/playthrough/rp-memory/write` 在整份客户端源码里**恰好出现 1 次**（保存那一处）⇒ ⛔ 没有第二条"替用户写回"的路 |
| — | 面板入口**排在手动手动确认之后** | 客户端新入口全在 `gate === 'ready'` 分支里 | 客户端台子：未确认分支里 `dma-rpmem-edit|zone|save|editor|deadzone-alert` **零命中** |

### §2.3 端点（新增，形状照本仓现成的抄）

| 端点 | 表 | 分派 | 说明 |
|---|---|---|---|
| `POST /playthrough/rp-memory/write` | `lib/index.js:3441` | `:3555` | 直接编辑。`ENDPOINTS 条目 1 处 + 分派 1 处` 由台子 **D7** 逐条钉死（三个端点各一条） |
| `GET/POST /playthrough/rp-memory/deadzones` | `lib/index.js:3442` | `:3556` | 读/写死区数据（POST 拒改走 400/409） |
| `GET /playthrough/rp-memory/deadzones/status` | `lib/index.js:3443` | `:3557` | 只读现状（⛔ 不写盘） |

- **与既有 `GET /playthrough/rp-memory` 同源**：三条都走同一份落点解析 `resolveRpMemoryDir()`（`:4751`）
  与同一份名字裁决 `rpMemoryNameOk`（**D4**：read/write/deadzones 对同一个坏名字口径完全一致）。
- **`err()` 形状 / `redact` 用法 / 状态码口径**照本仓现成的抄：读侧老口径（HTTP 200 + `ok:false`，
  与 `GET /playthrough/rp-memory` 一字不差），写侧拒改走真状态码（400 体坏 / 404 没有 /
  **409 拒改** / 413 太大 / 500 写盘失败），错误文案统一过 `redact`。

### §2.4 检测挂在哪（复用既有钩子，选最省的那条）

读代码后选的是 **`system-prompt/assemble`**（不是 `session/event`）——理由写在 `lib/index.js:4370-4378`：
本档的约束那一段（`mt:memoryHome`）本来就在这条瀑布上每轮装配一次，**看守跟它同一份文档读取**
（`deadzoneDocOf` 带 mtime 缓存），不必再引一条事件流、也不必每轮读两遍盘。
三条硬要求都在台子 C 节钉死（每轮至多一次 / 不阻塞 / 不每轮重写状态文件）。

---

## 2 §3 八对的证据（红/绿 + 反证写法）

命令：`node _selftest-deadzone.mjs` ⇒ **70 通过 / 0 失败**（下面按对摘录原文）。

| 对 | 相（绿） | 反证（红 ⇒ 同一条判据必咬） |
|---|---|---|
| **1 快照不差** | `✔ B1 相｜死区块未动 ⇒ status=ok、changed 空、每条都是 ok`<br>`✔ B1b 死区块的数量与身份如实（两条，都指 index.md、带快照 sha）` | `✔ B1c ★反证｜死区块里改一个字符 ⇒ 必告警（status=changed、指得出哪份文件哪一段、两个 sha 都在）`<br>改的是 `'不许替玩家做决定'` → `'不许替玩家做决 定'`（**一个空格**）；另加 `B1f`（改回原样 ⇒ 回 `ok`，⛔ 不是"改过一次永远红"） |
| **2 死区之外改** | `✔ B2 相｜只改非死区块 ⇒ 不告警（死区之外是作者的，随便改）` | `✔ B2b ★反证｜同一夹具下动死区块 ⇒ 立刻 changed（B2 不是永远绿）` |
| **3 用户编辑自动同步** | `✔ B3b ★★ 用户自己改的（含死区块被改）⇒ 保存后快照跟着走、状态回一致、**不告警**`<br>`✔ B3c 快照真的换了新 sha（刷新不是嘴上说说）` | **旧写法（挖掉那一步）= 同一段新文绕过端点直接改盘**：<br>`✔ B3d ★反证｜抽掉"保存时刷新快照"这一步（直接改盘、不走端点）⇒ 同一次操作必告警（这条判据在咬人）`<br>`✔ B3e ★ 结构性反证：保存那条路上**必须**有 refreshAfterEdit（挖掉它这条就不成立）`<br>（台子里挖的形式：把 `const next = deadzone.refreshAfterEdit(…)` 那一行从 `handleRpMemoryWrite` 体里替换掉 ⇒ 同一句断言不再成立） |
| **4 注入面** | `✔ B4b 相｜注入面：段里有**文件名 + 那段首行原文 + "只有追加权"那句**`<br>`✔ B4c ★ 死区**原文整段**不进注入（只给首行摘要 —— 别把上下文撑爆）`<br>**真实输出**（一次性取证脚本，已删）：<br>`<memoryHome>`<br>`本会话的剧情笔记目录：…\ch\pt\.roleplay-memory`<br>`一个周目一份 —— **只往这里写**，⛔ 不要写到别的周目目录。`<br>`⛔ 死区（作者预置，你只有追加权）：index.md 的「## 【必须遵守的核心规则】」「## 【H-scene 写作准则】」两段 —— 不许改写/覆盖/删除，要更新只能追加在它们之外。`<br>`</memoryHome>`<br>坏数据时（`corrupt`）：`⚠ 死区数据（.dma-deadzones.json）读不出来（corrupt）—— 作者设的禁改区这次没能生效，⛔ 别猜哪几段是死区。` | **反证 = 没有死区 ⇒ 那行不出现**：<br>`✔ B4e ★反证｜没有死区 ⇒ 那行**不出现**（注入面是"有事才说话"）`<br>另加 `B4d`（不是本会话的会话 ⇒ 段里一个字都不注） |
| **5 编辑落盘** | `✔ B5b ★ 盘上就是新文（逐字节）`<br>`✔ B5c ★ 写前备份真的落在原地，且内容是**改前**那一份（复制留档，⛔ 不销毁）`<br>`✔ B5c2 ★ 备份名带毫秒戳（同一秒里连点两次保存也不会互相盖掉备份）` | **挖掉备份那一步**：<br>`✔ B5e ★反证｜抽掉备份那一步：同一夹具下不备份就没有 .bak-（证明 B5c 咬的是那一步，不是别的东西）`<br>`✔ B5f ★ 结构性反证：备份必须发生在写盘**之前**（顺序反了就等于没备份）` |
| **6 反证｜路径与扩展名** | （本条本身就是反证）`✔ B6 ★反证｜路径穿越 / 绝对路径 / 别的扩展名 / 保留名 / 点开头 / 空名 ⇒ 一律拒（400/404），⛔ 不写出去`<br>`✔ B6b ★ 拒完之后盘上**一个字节都没变**，目录里也没多出文件`<br>`✔ B6c 读侧同口径：这些名字在 ?file= 上也一律拒（同一条裁决，⛔ 两处各写一遍）`<br>真 HTTP：`HTTP 400 {"ok":false,"error":{"code":"RP_MEMORY_BAD_FILE","message":"这个文件名不能写（只能是记忆库目录里的普通文件）"}}` | 夹具（10 个坏名字）：`'../../x.md'`、`'..%2Fx.md'`、`'C:\\secret.md'`、`'/etc/passwd'`、`'a.exe'`、`'notes.json'`、`'CON.md'`、`'.hidden.md'`、`'sub/x.md'`、`''`、`null`；目录外放了 `TOP-SECRET-DEADZONE` 那份，全程断言它没被读到/改到/夹带进任何响应 |
| **7 乐观锁** | `✔ B7 相｜sha256 与盘上不符 ⇒ **409** + 可读文案（含"先刷新再改"）`<br>`✔ B7b ★ 盘上文件一个字节没变（⛔ 绝不静默覆盖）`<br>真 HTTP：`HTTP 409 {"ok":false,"error":{"code":"RP_MEMORY_WRITE_STALE","message":"盘上已经变了（可能是模型刚写过，也可能是你在别处改过），先刷新再改 —— 你手里的那份没有覆盖上去"}}` | **挖掉乐观锁那一步**：<br>`✔ B7c ★反证｜把乐观锁那一步挖掉（同一夹具、同一形状的写入直落盘）⇒ "盘上没变"这句必红`（等价物：`writeFileSync(probe, '被覆盖了')` 之后读回来**确实**变了）<br>`✔ B7d ★ 结构性反证：乐观锁判据只认"重算 sha 再比"，⛔ 不认 mtime / 不认"文件被改过"` |
| **8 反证｜不自动还原** | `✔ B8 相｜死区被改 ⇒ 告警在（changed + 指得出哪一段）`<br>`✔ B8b ★★反证｜谁都不许替用户改回去：查完之后盘上**逐字节还是被改过的那一份**`<br>`✔ B8c ★★ 走完全部端点（现状/清单/加死区/原样保存）之后，那一份仍然是**用户/模型留下的原文**（没被悄悄还原）` | `✔ B8d ★ 结构性反证：告警那条路上**没有任何**"把快照写回文件"的调用`（同时扫 `handleRpMemoryDeadzoneStatus`、`runDeadzoneCheck`、`compareZones` 三处：零 `writeFileSync`） |

**另加的两节**（任务书没点名，但这两件事不钉住我不放心）：
- **C 接线纪律**（10 条）：`✔ C1 ★ 真接线｜这一轮的比对**不占本轮时间**：调 next() 那一刻还没有告警，让出事件循环之后才有`
  ／`✔ C2 ★ 每一轮至多一次`／`✔ C2b ★ 换一轮但结果没变 ⇒ 不刷状态文件、不重复告警`／
  `✔ C4 ★★ 不许每轮重写状态文件：那份文件连 at 都没动`／`✔ C5 ★ 结构性反证：看门那条路上**没有 await**`。
- **E 真机形状**（5 条）：把夹具换成**真机那样**（周目目录那份压根不存在、笔记与死区都在**共用那份**上）⇒
  `✔ E1`（面板落到 `base=workspace-root`）、`✔ E2`（注入那行照旧出现，并额外点明它在哪处目录）、
  `✔ E4`（编辑落在共用那份 + 备份也在那处）、`✔ E5`（用户改完仍"一致"，不误报）。

---

## 3 面板侧的前后对比（文字描述）

**改前**：「剧情大纲」档 = 一句防剧透说明 + `显示角色扮演记忆库（含剧透）` 按钮；确认之后是一串文件名，
点开哪一行才取哪一行的正文，**只读**（渲染成一篇 Markdown），整档上下没有一个写入口。

**改后**（同一个档，同一个门 —— 所有新入口都在**手动确认之后**才存在）：

1. **文件行**多了两个徽标：`🔒 N 段死区`（这份上有几条死区）与 `⚠ 死区不符`（这份上有段落被改了，
   红字胶囊那种）；点了确认之后面板顶部先出现一行安静的 `正在看死区有没有被改…`，随后：
   - **一致**：一行浅灰小字 `🔒 死区 2 段，与快照一致。（模型每轮会比对一次，最近一次：2026-09-23 22:10:12）`；
   - **不一致**：一块**红字**（`data-dma="deadzone-alert"`，`role="alert"`）：
     ```
     ⛔ 死区内容与快照不符（可能是模型改的，也可能是你在别处改过）—— 共 1 处：
     · index.md 的「## 【必须遵守的核心规则】」：快照 188f08a3e2c9… → 现况 dac5934d6c88…
     本插件只告警、不会替你改回去 —— 文件始终是你的。要它不再红：自己确认后改回原样、解锁这一段，
     或在下面用「✎ 直接改」保存一次（保存会让快照跟着走）。
     ```
   - **死区数据本身坏掉**（手改成非 JSON）：一块红字 `⚠ 死区数据（.dma-deadzones.json）读不出来（corrupt）
     —— 作者设的禁改区这次没能生效（约束没喂给模型、也没在比对）。请到那份文件里看一眼。`
2. **点开某一份之后**：正文不再是"一整篇 Markdown"，而是**逐块**渲染 —— 每块头上一条小字
   （`第 1 块 · ## 【必须遵守的核心规则】 · 34 字`）+ 一颗按钮。**没划过的块**给 `🔒 设为死区`；
   **已划的块**是**灰底**、带红胶囊 `🔒 死区 · 模型不许改`，按钮变成 `🔓 解锁`。
   点任一颗 ⇒ 发一条 POST（只带 `file` + 那段的 `sha256` + 那段的原文快照），回来后**就地刷新**
   这一份（锁标、灰底、字数、sha 全跟着变），并在块下方给一行绿字 `这一段已设为死区（模型不许改写/覆盖/删除）`。
3. **同一行右侧**多了 `✎ 直接改`（只有 `.md`/`.txt` 才有；太大而被截断的那一份**不给**这个入口，
   按钮上是 disabled + 悬停说明"免得把没显示出来的部分写没"）。点它 **就地**摊开一个等宽 textarea
   （初值 = 宿主刚给的那份正文，⛔ 不是面板自己缓存的老副本），下面三样：`保存这一份`、`放弃改动`，
   以及一行如实小字 `保存 = 用你看到的这份整份覆盖（先备份）；盘上要是已经变过 ⇒ 拒改并让你先刷新（乐观锁）。`
   - 保存成功：一行绿字 `已保存：67 字 / 165 字节 · sha c8b0543c41ac… · 备份 index.md.bak-2026-09-23T14-25-56-116 ·
     死区快照已跟着刷新 1 条（其中 1 条是你刚改过的那段）`（若那一段被你整段改掉，会多一句"已经解除"）；
     编辑器自动收起、正文刷新成盘上那一份。
   - 撞乐观锁（409）：一行红字 `没保存（盘上一个字节都没动）：盘上已经变了（可能是模型刚写过，也可能是你在别处改过），
     先刷新再改 —— 你手里的那份没有覆盖上去` —— **草稿留着**（⛔ 不悄悄丢），旁边多一颗
     `重新读取（丢掉我这份草稿）`；点了才重取盘上那一份（那颗按钮的措辞就把"会丢草稿"说清了）。
4. ⛔ 全档**仍然零 useEffect**：没有定时器、没有轮询、没有"进档就自动拉取"——所有取数都发生在
   用户动作里（确认 / 点开某一行 / 保存 / 划死区 / 打开文件夹）。

---

## 4 没做 / 不确定（**请部署后的真机核**）

1. **⛔ 真机上点下去到底行不行 —— 我没法下结论**：本单全程**没碰实机**（任务书 §4/§5），
   台子里的 HTTP + 假 ctx 是**本进程**起的，宿主进程里的真实接线（`systemPrompt` 段、
   `system-prompt/assemble` 监听、`storageDir` 可写）**只有部署 + 重启宿主之后才能验**。
   → 请你在真机上核这四件事：① 划一段死区后**下一轮**的提示词里出现那句约束（查看器的 `mt:memoryHome` 段）；
   ② 故意让模型重写那一段 ⇒ 面板红字 + 日志那条 warn；③ 面板里改一份笔记并保存 ⇒ 回执四项 +
   盘上 `.bak-` + 再查死区是"一致"（红利：**不误报**）；④ 编辑时先在外面改一次那份文件 ⇒ 保存应回 409。
2. **§2.1 ⑦ 的 ③「面板顶部那条状态」没做**（任务书写的是"若有现成的位置，**也可以**带一句"）。
   现成的位置只有顶栏那几颗按钮，要加就得**新开一条常驻请求**（面板一打开就查），
   与"⛔ 空闲时零定时器"的既有纪律冲突 ⇒ 本单把它留给档内的红字（那是 §2.1 ① 的硬要求，已做）。
3. **死区的两条已知语义**（都如实写进 `lib/deadzone.js` 头注，不是 bug，但你可能想知道）：
   ① 定位**只认 sha** ⇒ 两段**内容完全相同**的块算同一段（设一条两条都上锁、解锁一条两条都松）；
   ② 首行只是**告警文案**里的补充线索（sha 找不到时用它认"这一段被改了"），⛔ 不是定位依据。
4. **面板里编辑会把该文件的行尾统一成 LF**（浏览器 textarea 的既定行为）⇒ 一份 CRLF 的笔记
   在面板里保存一次就变 LF。内容不丢、有备份、死区快照跟着走（换行归一已处理，⛔ 不会因此误报），
   但**字节层面确实变了**。要保留 CRLF 请用系统编辑器。
5. **死区跟着目录走**（不是跟着"周目 id"）：真机上笔记落在那份**共用** `.roleplay-memory/` 时，
   死区也在那儿 ⇒ **同一工作区里别的周目也会看到同样的死区**。面板已经如实标出来
   （用共用落点时多一行小字），但这是个**语义**，你可能要决定"要不要只给本局用"（那就把文件放进周目目录那份）。
6. **共用 GLM 账号速率限制**：本单收尾没撞 `1302`（所有产物已落盘）。
7. **本单没有改**：`lib/collect*.js`（任务书明令）、`lib/rel-path-jail.js`（只调用）、
   预设 `preset/agent.cordis.yml`（预设侧那条铁律是你上一单做的，本单不动）、其它仓库一律未碰。

---

## 5 门与产物

| 命令 | 结果 |
|---|---|
| `node --check lib/index.js lib/client.js lib/deadzone.js` | 全部通过（无输出） |
| `node _selftest-deadzone.mjs` | **70 通过 / 0 失败** |
| `node _selftest-client.mjs` | **120 通过 / 0 失败**（改前 119） |
| `node _selftest-rp-memory.mjs` | 25 通过 / 0 失败（读侧那半边一行未动，照旧全绿） |
| `node _selftest-session-playthrough.mjs` | 36 通过 / 0 失败（`memoryHomeFor` / `sharedRpMemoryDir` 的口径没被破坏） |
| `node "D:\apps\dsh-tarven配置区\产物\memory-tools\_run-all-selftests.mjs"` | **68 passed / 0 failed**（含 glm-bridge 与 anima-rag 两个兄弟仓；那个脚本可读可跑，**未改**） |

`CHANGELOG.md` 的 `[Unreleased]` 已补一条（**⛔ 没有起新版本号**，仍是 `0.7.0`）。
本单**未** commit / 未 push / 未部署 / 未重启任何宿主 / 未安装到任何 profile。
