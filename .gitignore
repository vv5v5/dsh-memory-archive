# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Packed tarballs
*.tgz

# Editor / OS junk
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# NOTE: lib/ is intentionally NOT ignored — this package has no build step,
# lib/ is the published artifact and must be committed.

# 临时改文件的脚手架（一次性脚本，⛔ 不进仓库）
_tmp-*

# 台子/CI 的临时目录（失败中断时会漏在仓库根；⛔ 绝不进仓库）
_selftest-*-tmp-*
.st-ci-tmp-*
