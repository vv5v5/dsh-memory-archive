# 报告 · 记忆写入：**撞上限 ⇒ 一句明确的"该优化了"** ＋ 提示词轻量化（2026-09-26 01:34）

> 派单：`D:\apps\dsh-tarven配置区\04-派单\手工派单-任务书-记忆写入-撞上限提示优化+提示词轻量化-20260926.md`
> 本任务 id：`20260926-012632-记忆写入-撞上限提示优化+提示词轻量化`
> cwd = `D:\apps\deepseek\dsh-memory-archive`

## 0 一句话结论

**主活（记忆写入那一半）已落地并全绿**：撞单次上限时，模型收到的不再是「请拆成两次写」，
而是一次明确的**"该优化了"** —— ①撞的是哪个上限（数字给全）②为什么该优化 ③**三件可照做的动作**，
顺序说死 **先优化、再写**。判据一个字没动（同一道 `maxChars` 闸、同一个 `over-budget` reason、没新造阈值）。

**⚠️ 另一半（persona 轻量化）没落地 —— 被派单守卫拦下，不是没做**：`_glm-dispatch` 下发的守卫把
`dsh-memory-archive/preset/agent.cordis.yml` 列进**禁写**（`write.denyPath`），Edit 一试即被拦
（`禁止写入禁区路径：…preset/agent.cordis.yml`），守卫原话还写着「不要尝试这些路径」⇒ 人设正文**一个字节没动**，
也⛔ 没有绕道去写。那一半改成**干跑交付**：补丁在内存里跑过、拿**真判据**验过、前后字数算好，
全文在下面 §5，等一个允许写预设的派单照抄即可。

## 1 用户口径（逐字）

> 「重新设计下记忆写入功能，改为**到上限时自动提示需要优化**。同步**轻量化提示词**」

## 2 主活：撞上限的回执（`lib/index.js`）

### 2.1 改了什么

| 位置 | 前 | 后 |
|---|---|---|
| `lib/index.js:4258`（新增纯文案函数） | — | `noteOverBudgetText(decided)`：把三件事拼成一句话 |
| `lib/index.js:4406`（`over-budget` 那一支） | `throw new Error(\`这一段 ${…} 字符，超过单次上限 ${…} ⇒ 请拆成两次写（⛔ 不会截断硬写）\`)` | `throw new Error(noteOverBudgetText(decided))` |
| `lib/index.js:4305`（工具描述末句） | `… 超预算会被**拒收**（那时拆成两次写，不要硬塞）。` | `… 超单次上限会被**拒收**（那时**该优化笔记了**，回执会给三件事；先优化、再写）。` |
| `lib/index.js:4339`（`text` 参数说明） | `要写入的内容（超单次上限会被拒收 ⇒ 拆成两次写）；` | `要写入的内容（超单次上限会被拒收 ⇒ 那时**该优化笔记了**：先优化、再写）；` |
| `lib/memory-write.js:137/150-158`（注释，无行为） | `超预算 ⇒ {…, chars}（不截断硬写）`／「超了就让它自己拆成两次写」 | 真值表补 `maxChars`；注释写成新口径（回执由 `noteOverBudgetText` 写成"该优化了"）＋ §2.3 的结论 |

### 2.2 新回执原文（真工具跑出来的，原样）

参数是 `path:'notes.md', mode:'append', text:` 20001 个 `x`（上限 20000）：

```
这一段 20001 字符，超过单次上限 20000 ⇒ 拒收（⛔ 不会截断硬写）。
笔记在长胖 —— §5 里那几段本该「每轮替换、只留最新」。**先优化，再写**这一轮：
① 过时的条目 ⇒ `mode:'replace'` ＋ `text:''`（把 `find` 指到的那一段整个删掉；删除前照样先备份）；
② 最早那几条场记 ⇒ 压成**一行**留在原位，伏笔挪进 `world.md` 的「未解伏笔」；
③ `index.md` 你自己那几段（〔AI 自记〕 那两段）⇒ 按 §5 的骨架**整段换掉**，只留最新那份。
三件都做完还超 ⇒ 才拆成两次写（⛔ 不要硬塞）。
```

合计 291 字符。三件事的落点：**①数字**=第 1 行；**②为什么**=第 2 行（"笔记在长胖…每轮替换、只留最新"）；
**③怎么优化**=①②③ 三条；**顺序**=「**先优化，再写**」在第 2 行、"拆成两次写"退到第 5 行（最后一档退路）。

### 2.3 判据没动（任务书 §2.2）

- `over-budget` 的**条件**照旧（`lib/memory-write.js:155`：`text.length > maxChars` ⇒ 拒），**reason 名照旧**
  （⛔ 没新造）、**阈值照旧**（`MEMORY_WRITE_MAX_CHARS = 20000`，配置可改；⛔ 本单没改数值）、
  ⛔ 没加自动压缩/自动删除 —— 动手的永远是模型自己。
- 本单只动**文案**与**给模型的下一步**。自检里钉住这一点：★U①（三种 mode 一律 over-budget，⛔ 没给
  `replace` 开小门）、★U①b（边界仍按配置那个数：500 放行 / 501 拒）。
- `lib/index.js` 里那个自记标记字面**仍然只从常量来**（`NOTE_MARK`，⛔ 没在本文件重打；★M⑧b 照旧绿）。

## 3 任务书 §2.3 的结论：`replace` 的 `text` 也受同一个 `maxChars` 管 —— 但**瘦身本来就过得了**（⛔ 不需要动）

**查清了，也钉住了（★U② 那一组）：**

1. **是同一个上限**：那道闸**不分 mode** —— `overwrite` / `append` / `replace` 的 `text` 一律按同一个
   `maxChars` 判（★U①）。所以"把一大段原样抄回去当 `text`"照样被拒（★U②c）。
2. **但瘦身不受影响**：闸看的是 **`text` 的长度**，⛔ 不是结果文件的长度、也不是 `find` 的长度
   ⇒「把一整段 `replace` 成一小段」**永远放行**。实测（★U②）：`find` 指到 **30000 字**的那一大段、
   `text` 只 13 字 ⇒ 闸放行；落地后文件里那一大段没了、别处逐字还在，而**文件本身仍有 3 万字**
   （夹具里作者那一大节没动）—— 也就是说"文件远超上限"这件事**不构成拒收理由**。
3. **要不要动：不动，而且不该动**。新回执里那三条出路（`replace` 成空 / 场记压成一行 / 按骨架整段换掉）
   每一条都**要靠瘦身那一脚**才落地；若哪天把它改成"看结果文件总长"，瘦身会被它一并拒掉 ——
   自检里就有一条**反证**钉着这件事（★U②反证：模拟"看文件总长"的写法 ⇒ ★U② 必红）。
   文件级的体量纪律本来就不在这一层（那是 persona §5 的 6000 字口径 ＋ 新回执里的话）。

## 4 自检（本单新增/改动的四个文件）

| 台子 | 前 | 后 | 新增的都是什么 |
|---|---|---|---|
| `_selftest-memory-write.mjs` | 99/0 | **111/0** | ★U①/①b/①反证（闸不分 mode、上限只有一个来源、⛔ 没给 replace 开小门）；★U②/②b/②反证/②c（**瘦身本来就过得了** ＋ 闸看 `text` 不看文件总长 ＋ 不为瘦身放宽判据）；★E9/E9b/E9c（**真工具**那侧：三件事都在 ＋ 顺序"先优化、再写" ＋ **反证**：剪掉"怎么优化"那段、或拿**上一版那句**来算 ⇒ 同一条必红）；★E9d（描述指向优化、⛔ 不再写"拆成两次写"）；★E9e（这一次拒收**盘上零变化**） |
| `_selftest-preset-persona.mjs` | 75/0 | **78/0** | **P18**（**文件级"该优化了"**：`notes.md` 超过约 6000 字／3000 token ⇒ 最早场记压成一行**留在原位**、伏笔移进 `world.md` 的"未解伏笔" ＋ 位置断言）—— 回执指的就是这条判据，而它**此前没被钉过**；锚点只取几个词，轻量化改措辞时不会误红 |
| 老判据 | — | 一条没删 | ⛔ 没换锚、没删判据（P1–P17 全在，且判据条数只增） |

**整套门**（`node D:\apps\dsh-tarven配置区\产物\memory-tools\_run-all-selftests.mjs`）：

```
=== 71 passed / 1 failed ===
失败：anima-rag/_selftest-playthrough-isolate.mjs
```

同一批 **72** 个台子（与任务书那个基线同数）。**唯一的红与本单无关**，是**别人的真机数据锚过期**：

- 那个台子在 `D:\apps\deepseek\dsh-anima-rag`，**一个字节都不读我们的仓库**（grep `dsh-memory-archive` 零命中）；
  它 ⑥ 数的是**真向量库**：`D:/apps/SillyTavern-Launcher/SillyTavern/plugins/anima-rag/vectors/dsh-memory/index.json`。
- 锚写着 **11** 条，现在是 **12** 条（`12 !== 11`）；该 index.json 的 mtime = `2026-09-26 00:38`（本地）
  —— 在**本单动手之前**，是用户今晚自己又入库了一条。它自己的断言消息就是「真库条数变了 ⇒ 请重核本锚的期望值」，
  且已被重核过两次（9-24 重核到 4、9-25 重核到 11，这是第三次）。
- ⛔ 本单**改不了它**：派单守卫只许写进 `dsh-memory-archive`（那文件在 `dsh-anima-rag`）。
  **要它变绿**：把那 4 处 `11` 改成 `12` —— `:76` 的标签、`:90` `assert.equal(items.length, 11, …)`、
  `:95` `assert.equal(deny.length, 11, …)`，＋ `:78`/`:93` 那两句重核注记补一行「★ 2026-09-26 重核：11 → 12，
  不是代码坏，是用户又玩了一轮」。判据本身（deny = 没有本周目标签的全部）一字不用动。

## 5 被拦下的那一半：persona 轻量化（补丁已验证，等一个允许写预设的派单）

### 5.1 拦截证据（原话）

```
禁止写入禁区路径：d:\apps\deepseek\dsh-memory-archive\preset\agent.cordis.yml
（命中 d:\apps\deepseek\dsh-memory-archive\preset\agent.cordis.yml）（dsh-glm-bridge 守卫 · write.denyPath）
【_glm-dispatch 边界】… 禁止写 … / D:\apps\deepseek\dsh-memory-archive\preset\agent.cordis.yml。不要尝试这些路径 …
```

⇒ 本单**没有**改人设；`preset/agent.cordis.yml` 的 mtime 没被碰过（⛔ 也没跑同步脚本、⛔ 没重启宿主）。

### 5.2 前后字数（按任务书 §3 的「净字数只许减、不许增」）

| 量 | 前 | 拟改后 | Δ |
|---|---|---|---|
| **persona 段**（`prefix: |-` → 下一个挂载行，与台子同口径） | **8977** | **8906** | **−71** |
| **§5 全节** | **2848** | **2777** | **−71** |
| 「更新纪律」那一块（本单新增内容就落在这里） | 735 | 848 | +113 |

口径说明：任务书要的是"persona 段"的净字数 ⇒ **−71**（减了）；「更新纪律」那一块**涨 113** 是必然的
——新规矩（什么时候该优化 ＋ 三件事）就写在它里面，而**付钱的**是 §5 里那几处重复解释
（下面 E1/E2a/E2b/E3/E5/E6/E7 逐条注明了"重复在哪"）。⛔ 没新增段落、⛔ 没删任何一条判据。

### 5.3 补丁（8 处替换，逐处在正文里**恰好命中一次**）

**E1** 删 §5 里与 §1★1 重复的那一条（`| 段不存在 | — | 本会话未归入周目… |` 那一格已写全）：

```
old:      段没出现 ⇒ 按 §1 告知玩家并给重进方式，⛔ 不猜、不自造目录。  
new: （整行删掉）
```

**E2a** `index.md` 那条：枚举改成"上面骨架那两段"（骨架块就紧挨在它上面）：

```
old: **目录＋最近进展（几句）＋当前时间地点＋下次续写提示（一句）＋导演笔记**。
new: **目录 ＋ 上面骨架那两段**。
```

**E2b** `index.md` 那条：末尾这句与 `world.md` 那条重复（那份写着"底层真相、设局者只进 `index.md` 的导演笔记"）：

```
old: 导演笔记＝你自己的备忘（底层真相、设局者、要铺未铺的线）。
new: （整句删掉）
```

**E3** 更新纪律第 1 条：`index.md` 前四段 → 与骨架对齐（骨架只两段）：

```
old: （`index.md` 前四段、`notes.md` 的场记、`characters.md` 的人物与关系）
new: （`index.md` 的「最近进展」、`notes.md` 的场记、`characters.md` 的人物与关系）
```

**E4** 更新纪律第 4 条：6000 字那条**吸收**进新的「该优化就优化」（一到两句、三件事、先优化再写）：

```
old: - `notes.md` 超过约 6000 字（或 3000 token，以先到为准）⇒ 最早场记压成一行**留在原位**，伏笔移进 `world.md` 的“未解伏笔”。
new: - ★ **该优化就优化**（`notes.md` 超过约 6000 字／3000 token，以先到为准；`memory_write` 报"撞单次上限"时同理）：①过时条目 `replace` 成空；②最早场记压成一行**留在原位**、伏笔移进 `world.md` 的“未解伏笔”；③`index.md` 你自己那几段**按上面的骨架整段换掉**（只留最新那份）。**先优化，再写。**
```

**E5** 铁律那条尾：`⛔ 不用 overwrite` 在同一块里已说过两遍（舍第三遍）：

```
old: ⛔ 不用 `overwrite`；作者的维护要求照旧保留。
new: 作者的维护要求照旧保留。
```

**E6** 仲裁那条尾：`仍⛔ 不许你自己 overwrite` 与上面那条（已存在的文件一律不许 overwrite）重复：

```
old: 照改后版本演，仍⛔ 不许你自己 `overwrite`。
new: 照改后版本演。
```

**E7** "拿不准是谁的"那一条：与「判作者」那条（"含你没写过的规则／准则／目录 ⇒ 当**既定事实**供着"）重复：

```
old:       · **拿不准是谁的** ⇒ 先 `read`：有你不认识的、不是你写的 ⇒ 当既定事实。
new: （整行删掉）
```

### 5.4 这个补丁**干跑验过**（⛔ 只写盘前那半步没做）

- 8 处替换**逐处恰好命中一次**（脚本对"命中 ≠ 1 次"一律报错，不会静默漏掉）。
- **现有判据的锚点一个不丢**：P1–P17 那一整套（含 P17 自记标记字面两处一致）＋ 铺盘脚本
  `_update-roleplay-preset.mjs` 的 5 个正锚（`作者的 ⇒ 一个字不动`、`写本轮时删掉上一轮那版`、
  `段首独占一行`、`上一轮那一行标记 ＋ 紧跟的那个标题行`、`就只有这两段，照抄`）全在；
  旧口径负锚（`只有追加权` / `没有"覆盖权"` / 已删的「回复格式」那节）一个都没回来。
- **拿真判据跑过**：把补丁后的正文落到一份临时件、把台子的读入路径换过去
  ⇒ `_selftest-preset-persona.mjs` **78/0 全过**（含 §5 里那几条位置断言）。
- **配套新判据 P19 也验过**（与补丁**同时上**）：补丁后 **80/0**；打回**现状**则 P19 **必红** ⇒ 它会咬人：

```js
// ── P19（2026-09-26）：**撞上限 ⇒ "该优化就优化"**（三件事 ＋ 先优化、再写）—— 与人设那一半补丁**同时上** ──
{
  pin('P19 ★ 「该优化就优化」：什么时候 ＋ 优化哪三件（`memory_write` 撞上限，或 `notes.md` 过 6000 字）', [
    '**该优化就优化**',
    '`memory_write` 报"撞单次上限"时同理',
    '①过时条目 `replace` 成空',
    '③`index.md` 你自己那几段**按上面的骨架整段换掉**（只留最新那份）',
    '**先优化，再写。**',
  ])
}
```

### 5.5 落地指引（给下一个能写预设的派单）

把下面这段存成 `_tmp-apply-persona-light.mjs` 放进仓库根、`node _tmp-apply-persona-light.mjs`（**默认干跑**）
⇒ 先看它报"净字数 −71 / 命中 8/8 / 台子 78 过 0 失败"，再加 `--apply` 真写。
（加了 P19 之后，两种跑法分别应是 **80/0** 与 **79/1**。）

```js
// 人设轻量化补丁：E1–E7 八处替换（⛔ 逐处必须恰好命中一次）。默认干跑；--apply 才写盘。
// 写完自检：① persona/§5 净字数只减不增；② 旧口径负锚不许回来；③ 拿**真判据**跑补丁后的正文。
import { readFileSync, writeFileSync } from 'node:fs'
import { spawnSync } from 'node:child_process'

const APPLY = process.argv.includes('--apply')
const FILE = 'preset/agent.cordis.yml'
const EDITS = [
  ['E1 §5 里与 §1★1 重复的那一条',
    '      段没出现 ⇒ 按 §1 告知玩家并给重进方式，⛔ 不猜、不自造目录。  \n', ''],
  ['E2a `index.md` 那条：枚举改成"上面骨架那两段"',
    '**目录＋最近进展（几句）＋当前时间地点＋下次续写提示（一句）＋导演笔记**。',
    '**目录 ＋ 上面骨架那两段**。'],
  ['E2b `index.md` 那条：末尾与 `world.md` 那条重复的一句',
    '导演笔记＝你自己的备忘（底层真相、设局者、要铺未铺的线）。', ''],
  ['E3 更新纪律第 1 条：与骨架对齐',
    '（`index.md` 前四段、`notes.md` 的场记、`characters.md` 的人物与关系）',
    '（`index.md` 的「最近进展」、`notes.md` 的场记、`characters.md` 的人物与关系）'],
  ['E4 更新纪律第 4 条：吸收进「该优化就优化」',
    '- `notes.md` 超过约 6000 字（或 3000 token，以先到为准）⇒ 最早场记压成一行**留在原位**，伏笔移进 `world.md` 的“未解伏笔”。',
    '- ★ **该优化就优化**（`notes.md` 超过约 6000 字／3000 token，以先到为准；`memory_write` 报"撞单次上限"时同理）：'
    + '①过时条目 `replace` 成空；②最早场记压成一行**留在原位**、伏笔移进 `world.md` 的“未解伏笔”；'
    + '③`index.md` 你自己那几段**按上面的骨架整段换掉**（只留最新那份）。**先优化，再写。**'],
  ['E5 铁律那条尾：`⛔ 不用 overwrite` 的第三遍',
    '⛔ 不用 `overwrite`；作者的维护要求照旧保留。', '作者的维护要求照旧保留。'],
  ['E6 仲裁那条尾：与"一律不许 overwrite"重复',
    '照改后版本演，仍⛔ 不许你自己 `overwrite`。', '照改后版本演。'],
  ['E7 "拿不准是谁的"那一条（与「判作者」那条重复）',
    '      · **拿不准是谁的** ⇒ 先 `read`：有你不认识的、不是你写的 ⇒ 当既定事实。\n', ''],
]
const src = readFileSync(FILE, 'utf8')
let out = src
const bad = []
for (const [name, oldS, newS] of EDITS) {
  const n = out.split(oldS).length - 1
  if (n !== 1) { bad.push(`${name}：正文里命中 ${n} 次（应为 1）`); continue }
  out = out.split(oldS).join(newS)
  console.log('✓ ' + name)
}
if (bad.length > 0) { console.error('✘ 先修这些再跑：' + bad.join('；')); process.exit(1) }

const personaSlice = (s) => { const i = s.indexOf('prefix: |-'); return s.slice(i, s.indexOf('\n- id:', i)) }
const before = personaSlice(src).length; const after = personaSlice(out).length
const s5 = (s) => { const p = personaSlice(s); const a = p.indexOf('## §5'); return p.slice(a, p.indexOf('## §6', a)) }
console.log(`persona 段：${before} → ${after}（${after - before}）`)
console.log(`§5 全节：${s5(src).length} → ${s5(out).length}（${s5(out).length - s5(src).length}）`)
if (after > before) { console.error('✘ 净字数涨了 —— 不许（任务书 §3）'); process.exit(1) }
const pOut = personaSlice(out)
for (const a of ['只有追加权', '没有"覆盖权"']) {
  if (pOut.includes(a)) { console.error('✘ 人设里旧口径回来了：' + a); process.exit(1) }
}
for (const a of ['作者的 ⇒ 一个字不动', '写本轮时删掉上一轮那版', '段首独占一行',
  '上一轮那一行标记 ＋ 紧跟的那个标题行', '就只有这两段，照抄']) {
  if (!out.includes(a)) { console.error('✘ 铺盘脚本的正锚丢了：' + a); process.exit(1) }
}
// 拿**真判据**跑补丁后的正文：临时落件 + 把台子的读入路径换过去
writeFileSync('.dma-persona-patched.yml', out, 'utf8')
const TEST = readFileSync('_selftest-preset-persona.mjs', 'utf8')
writeFileSync('_tmp-persona-test-patched.mjs',
  TEST.replace("path.join(repo, 'preset', 'agent.cordis.yml')", "path.join(repo, '.dma-persona-patched.yml')"), 'utf8')
const r = spawnSync(process.execPath, ['_tmp-persona-test-patched.mjs'], { encoding: 'utf8' })
console.log(String(r.stdout).trim().split('\n').slice(-1)[0])
if (r.status !== 0) { console.error('✘ 真判据在补丁后的正文上没过 —— 先别写盘'); process.exit(1) }
if (APPLY) { writeFileSync(FILE, out, 'utf8'); console.log('✔ 已写 ' + FILE) } else { console.log('（干跑；要写盘加 --apply）') }
```

## 6 ⛔ 本单没做 / 没碰

- ⛔ 没改 `maxChars` 的数值（`MEMORY_WRITE_MAX_CHARS = 20000` 照旧）、⛔ 没新造阈值、⛔ 没新造 reason 名；
- ⛔ 没加自动压缩 / 自动删除（一切仍由模型自己调 `memory_write`）；
- ⛔ 没碰 `preset-modules/`、面板 `lib/client.js`、其它档（摘要/原文/向量/压缩）、Agent 编辑器；
- ⛔ 没跑同步脚本（`_update-roleplay-preset.mjs` / `_materialize-preset-modules.mjs`）、⛔ 没重启宿主、⛔ 没提交；
- ⛔ **没有**绕道去写被守卫禁写的那三个路径（`C:\Users\w\.dsh`、`C:\Users\w\.zcode`、`dsh-anima-rag` 之类），
  也没有为"让门变绿"去改别人的台子。

## 7 本单改到的文件（与任务书那六条的对照）

| 文件 | 状态 |
|---|---|
| `lib/index.js` | ✅ 改了（新 `noteOverBudgetText` ＋ 描述/参数两处 ＋ throw 一处） |
| `lib/memory-write.js` | ✅ 改了（**只有注释**：`over-budget` 契约 ＋ §2.3 的结论） |
| `_selftest-memory-write.mjs` | ✅ 改了（★U 4 条 ＋ ★E9 5 条；99 → 111/0） |
| `_selftest-preset-persona.mjs` | ✅ 改了（P18 一条；75 → 78/0） |
| `CHANGELOG.md` | ✅ 改了（`## [Unreleased]` 顶部新增 2026-09-26 一节） |
| `preset/agent.cordis.yml` | ⛔ **没改**（被派单守卫禁写）—— 补丁与前后字数见 §5 |
