# 交付报告 · 归档摘要**按段切片**（一个叙事段 = 一条条目 —— 检索的粒度就是「一条文件」）

派单：#20260922-173322-摘要按段切片（任务书 `手工派单-任务书-摘要按段切片-20260922.md`）
cwd：`D:\apps\deepseek\dsh-memory-archive` ｜ 分支 `main` ｜ ⛔ 未 commit / 未 push / 未部署 / 未碰真机

本轮我改了 4 个文件：

| 文件 | 改了什么 |
|---|---|
| `lib/collect.js`（A1，本单**唯一允许改的既有文件**） | 新增**追加式** `summaries: [{id?, text, model?, meta?}, …]`：一次调用 = 楼层写一遍 + 每份一张 `.md` + 索引追加 N 条 + `manifest.count` = 追加后的条目数。老路（只传 `summary`）逐字未动 |
| `lib/collect-scan.js` | `beautifyModelSummary` 多回**段数组**；`selectModelSummary` 字节上限改**逐段各自判**；`planScan` 结构化那一路**逐段落盘**；`replanForRegion` 照原样带 N 份；`collectOnce` 的 A1 适配面给每份一个 `mt-<from>-<to>-N` |
| `_selftest-collect-scan.mjs` | 新增**第 17 节**（任务书 §3 的六对，真链路 + 四个反证副本）与**第 18 节**（逐段字节上限 + 反证）；现有 12–16 节一行未删（130 ⇒ 155 通过） |
| `_selftest-collect.mjs` | 新增 A14–A18（A1 侧的追加式 / 老路形状钉死 / 两路并存 / 落点唯一 / 逐段判重）+ 突变 `MUTATE=9`（12 ⇒ 17 绿） |

⚠️ 工作区里还有**上一单**（#20260922-163935 摘要美化+tags标签化）与**上上单**（#20260922-161203 收纳区间）
**未提交**的改动（`lib/index.js` / `lib/summarize.js` / `_selftest-summarize.mjs` 以及 CHANGELOG 里那两条）
—— 那是拿两份报告的产物，**我一个字没动**（§2.3 有 `git diff --stat` 的同数证据）。`lib/collect.js`
在开工时是干净的（没有未提交改动），所以我的 A1 改动**只含本单**。

一句话结论：结构化那一路**一个叙事段 = 一条摘要条目**（一条 `.md` + `index.json` 一条 entry，每段带
**它自己的** tags）；楼层仍只写一遍（一次调用提交 N 份，⛔ 不是调 N 次 A1）；机械兜底与 fail-open 两路
照旧一条；A1 老路（只传 `summary`）的 plan / 落盘字节**逐字节不变**。

---

## 1 §2 逐条对照表（落在哪一行 / 怎么证的）

### 2.1 A1 加一条**追加式**能力：一次收「一组摘要」

| §2.1 要求 | 落在哪儿（行号） | 怎么证的（命令 + 真实输出） |
|---|---|---|
| 入参新增可选 `summaries: [{id?, text, model?, …}, …]`（数组，每条 = 一份摘要条目与它自己的落点） | `lib/collect.js:18`（头注）、`:340` `summaryEntriesOf()`（归一化 + 校验，**唯一一份实现**，校验与 planCollect 共用） | `_selftest-collect.mjs` **A15**（3 份 ⇒ 索引 +3）、**A17**（缺 id / id 重复 ⇒ `COLLECT_INVALID`）；`_selftest-collect-scan.mjs` **17a**（真链路 3 份 ⇒ 3 条 entry） |
| `summary`（单份）那条老路**逐字保持原样**；A1 自检台**不许改断言**；老路字节行为不变 | `lib/collect.js:427` `const summaryEntries = (… : [{ id: summaryId, path: summaryPath, text: input.summary.text, … }])` —— 老路折成**一份的数组**走**同一条**代码路径；`:526`–`:533` plan 的五个老字段按 `oneShot` 分别给 | ① `_selftest-collect.mjs` 的 A2/A3/A11/A12 **一行断言没改**、原样全绿（`17/17 绿`）；② **一次性对照脚本**（对照物 = `git show HEAD:lib/collect.js`，跑完即删）：`SAME plan 键序` / `SAME plan 全文（含 hash / expectedRevisions / willWrite）` / `SAME apply 的 written 清单` / `SAME 假 Tavern 盘上每个文件的逐字内容` / `SAME hash`；③ **A14** 把老路的 plan 键集合**冻结**（多/少一个键就红） |
| 一次调用 = 楼层写一遍 + 每段一条 `.md` + `index.json` 追加 N 条 + `manifest.summaries.count` = 追加后的条目数 | `:429`、`:441`（逐份登记 willWrite / expectedRevisions）、`:464`（逐份判重）、`:583` `buildIndexDoc(existingDoc, entryOrEntries)`（**一条或一组**，一组按给定顺序逐条追加）、`:688`（逐份写正文）、`:704`（索引按 N 份重建）、`:732`（`mDoc.summaries.count = newIndex.entries.length` —— 口径不变，只是 +N） | **A15**：3 份 ⇒ `after.entries.length === 3`、`manifest.summaries.count === 3`、每份正文逐字（`第 1 份正文应逐字是那一段`）；真链路侧 **17a/17b**（index.json 里真实 3 条、字数 563/563/564、合计 1690） |
| **限一份实现**：`summaryPathOf` 仍是唯一落点口径；`expectedRevisions` 里每段的路径都要登记 | `:340`–`:374`（`summaryEntriesOf` 内部**只调** `summaryPathOf(target, s, range)`，两张路径相同就拒）、`:447` `expectedRevisions[e.path] = …`（逐份登记） | **A15**：`assert.ok(segPaths.every((p) => hasOwnProperty(plan.expectedRevisions, p) && plan.expectedRevisions[p] === null))`；**A18** 覆盖模式下逐份取**真身 sha**；**17h**：真 HTTP 的 PUT 流水里**3 个摘要路径各 PUT 一次** |
| 传了 `summaries` 又传 `summary` ⇒ 可读地报错（`COLLECT_INVALID`），⛔ 不猜 | `lib/collect.js:327`（文案常量）、`:294`（`validatePlanInput` 那条路）、`:352`（planCollect 那条路，同一个 `summaryEntriesOf`） | **A16**：`planCollect` 与 `validatePlanInput` **两处都拒** `COLLECT_INVALID` 且文案含「只能给一个」；**17l/17m**：真链路侧同一条断言，且**反证副本 D**（挖掉守卫）⇒ `{"willWrite":5,"summaries":2}` = **不再报错、静默挑一个** ⇒ 断言必红 |
| ⛔ `lib/ami-tags.js` 对外语义不变 | 该文件**零改动** | `git status --porcelain` 里没有它；`_selftest-tags.mjs` 与第 16 节的词表冻结断言（16f）照旧全绿 |

### 2.2 collect-scan：结构化那一路**逐段落盘**

| §2.2 要求 | 落在哪儿 | 怎么证的 |
|---|---|---|
| `beautifyModelSummary` 同时回**段数组**（判据只此一处，⛔ 不在 planScan 里再解析 JSON） | `lib/collect-scan.js:547`（结构化：`segments: bag.segments`）、`:559`（fail-open：**一段** = 整篇原文）、`:579` `collectItemTags()`（**段级** tags + 并集视图，一次遍历同时产出） | **17a/17b**（三条 entry 的正文逐字 = 那三段散文、无 JSON 外壳）；**18b**（第 2 段照常落散文 + 段级 tags） |
| `planScan` 用 `summaries` 一次提交，**每段一条** | `:899` `if (placed.length > 1) entry.summaries = placed`、`:922` `...(… ? { summaries: entry.summaries } : { summary: entry.summary })` | **17a**（真链路 ⇒ index.json **真实追加 3 条**、文件 `mt-0000-0001-1/2/3.md`） |
| `id = mt-<from4>-<to4>-<N>`（N 从 1 起；**多段一律带后缀**；只有一段时**不带后缀**） | `:1312` `summaryIdFor(floors, segmentNo)`（`Number.isInteger(segmentNo) && >= 1` 才加 `-N`）、`:1445`–`:1461`（适配面逐份 `id: summaryIdFor(floors, i)`） | **17a**（3 段 ⇒ `-1/-2/-3`）、**17d**（1 段 ⇒ `mt-0000-0001.md`，**不带后缀**） |
| `text` = 那一段的散文；`tags` = **那一段自己的**收编结果（⛔ 不是并集） | `:884`–`:886` `placed`（`{ text: s.text, tags: s.tags, meta: { ...sel.meta, tags: s.tags } }`） | **17e**（三段各 `[Serious]` / `[Romantic]` / `[Suspense]`）；**反证副本 B**（tags 改回并集）⇒ 三条全变并集 ⇒ 必红 |
| `fromFloor/toFloor` = 该区间的范围（各段共用，⛔ 不硬编） | `:1455`（适配面：所有份共用 `range: { fromFloor: floors[0].floor, toFloor: floors[last].floor }`） | **A15**：`assert.ok(after.entries.every((e) => e.fromFloor === 0 && e.toFloor === 1))` |
| `model` / `compactionId` / `eventSeq` 等 meta 各段一致 | `:886` `meta: { ...sel.meta, tags: s.tags }`（同一份 `sel.meta` 只换 tags） | **A15**：`after.entries.map(e => e.eventSeq) === [100,101,102]`（白名单字段逐段带上）；`model` 各段一致 |
| 机械兜底（prune）与 fail-open 两路 ⇒ **照旧一条**（⛔ 不硬造分段） | `:559`（fail-open 的 `segments` 恒为**一段**）、`:899`（只有 >1 段才走 `summaries`）、`:910`–`:920`（mechanical 分支原样） | **17j**（fail-open：条目 1 条、正文 = 整篇原文）、**17k**（prune：条目 1 条 + `no-model-summary` 播报） |
| 逐段的字节上限：**每段各自判**；某段超限 ⇒ 那一段回落机械 + 播报 `model-summary-too-big`（其余段照常）；区间级旧判据**退役** | `:472`–`:477`（`selectModelSummary` 里**只算一次** `tooBig`；全段都超限才整条回落）、`:883`–`:888`（planScan 据结果换正文 + 播报） | **18a**（第 1 段超 1 MiB、第 2 段没超 ⇒ `kind` 仍 `model-summary`）、**18b**（那一段正文 = 机械正文、tags/meta 留空）、**18c**（播报点名第 1 段）；**反证**：换回**区间级**那一条 ⇒ `kind=mechanical(model-summary-too-big)`、`segments=无` ⇒ 18a/18b 必红 |
| 对外的 `warnings` / `skipped` 原因码枚举**不变** | 枚举常量与分支一字未改：`no-model-summary` / `ambiguous-coverage` / `model-summary-too-big` / `prompt-echo` / `tags-partial` / `tags-empty` / `summary-plain` | `_selftest-collect-scan.mjs` 第 10–16 节（枚举与文案的老断言）**原样全绿**；新增的段级播报只**多**一条带 `model-summary-too-big` 字样的文案（18c），没有新码 |
| ★ `applyScan` 的**现取计划**也要带全 N 份 | `:1029`–`:1036`（`replanForRegion`：有 `region.summaries` 就照原样带过去） | **反证副本 C**（把现取计划的段丢掉）⇒ 落库静默缩回 1 份（`{"entries":[["mt-0000-0001.md",1692,…]]}`）⇒ 必红。**这是本单最容易漏的坑**：计划的 `willWrite` 全对，落库却少写 N-1 份 |

### 2.3 ⛔ 别的不许动 —— 逐项核对

| 不许动的东西 | 证据 |
|---|---|
| 区间判据 `findShadowRegionsFromEvents` / 楼的三条排除 `mapRegionToFloors` / `regionContentHash` | 三个函数**一字未改**（`git diff lib/collect-scan.js` 里没有它们的 hunk）；第 13 节那批老断言（13a–13p）原样全绿 |
| 台账形状 | `readLedger` / `writeLedger` / `emptyLedger` 未出现在本轮 diff 里；第 5 节的幂等断言（含"台账损坏""原子写"）全绿 |
| plan/apply 语义 | 只有**追加**（多一份数组那条路）；老路的 `willWrite` / `expectedRevisions` / `indexBeforeSha` / TTL / 双读乐观锁一个字未改（A5/A6/A11/A12 原样绿） |
| `archive/floors` 的形状与楼号连续性 | `floorDocText` / `floorFileName` 未动；**17g**：一次调用后楼层文件 2 个、每个路径**只 PUT 一次**、无"重复"warning |
| HTTP 端点表与状态码 | `statusOf()` / `errBodyOf()` / 路由表未动；A1 的 `handleCollectPlan` 只多**观察**了几个摘要路径（`:1026`，`observeArchive` 的 `summaryPaths` 数组），端点与状态码不变；第 12 节的端到端（含 `/collect/auto`）原样绿 |
| `looksLikePromptEcho`（按字数判） | `lib/summarize.js` **本单没碰**（见下） |
| `dsh-anima-rag` 一行都不许碰 | 全量门里 anima 的 7 个台子照旧全绿；我**只读**了它的 `lib/index.js` 的入库口径（`autoIngestOnce` 整文件一条切片），没写过一个字节 |
| 上一单 / 上上单的文件 | `git diff --stat -- lib/index.js lib/summarize.js _selftest-summarize.mjs` ⇒ `39 +-` / `16 +-` / `39 ++`，与**开工时快照逐字相同**（我一个字没动） |

---

## 2 §3 六对自检的红/绿证据（反证一律**真把旧写法改回副本里跑一遍**）

全部在 `_selftest-collect-scan.mjs` **第 17 节**（`:1345` 起），走**真链路**：
`collectOnce`（真接线）→ `planScan`/`applyScan` → **真 A1** `lib/collect.js` → **真 HTTP 的假 Tavern**。
断言的是 `index.json` 里**真实追加了几条**、`.md` 里**逐字**是什么（⛔ 不是源码字符串断言）。
夹具：三段散文合计 **1690 字**（对齐真机 `mt-0000-0167.md` 那条的读数），每段带自己的 vibe。

```
$ node _selftest-collect-scan.mjs
PASS 17a ① 相｜3 段 ⇒ index.json 追加 **3 条** entry、文件 mt-0000-0001-1/2/3.md、正文各是**那一段**
PASS 17b ① 相｜每份 `.md` 的字数 = 该段的字数（563/563/564）、三份不重不漏 = 1690 字
PASS 17c ① ★反证：落盘改回「拼成一条」⇒ 条目数 = 1（一条 1692 字）⇒ ① 的判据 slSliced 必红
PASS 17d ② 相｜只有一段 ⇒ 文件名**不带**后缀（mt-0000-0001.md）、条目 1 条、正文是那一段
PASS 17e ③ 相｜tags 段级：三条 entry 各是那一段自己的 [Serious] / [Romantic] / [Suspense]
PASS 17f ③ ★反证：tags 改回「各段并集」⇒ 三条 entry 的 tags 全变成 [Serious, Romantic, Suspense] ⇒ ③ 的判据 slSegTags 必红
PASS 17g ④ 相｜一次调用：楼层**每个路径只 PUT 一次**（2 楼 = 2 个 PUT，没有重复写同一楼）
PASS 17h ④ 相｜一次调用：摘要正文 **3 个路径各 PUT 一次**（N 份 = N 个文件，⛔ 不是写 N 遍同一份）
PASS 17i ④ ★反证：现取计划丢段 ⇒ 落库静默缩回 1 份 ⇒ ① 的判据 slSliced 必红
PASS 17j ⑤ 相｜fail-open（解析不出的原文）⇒ 条目数 = 1、正文是**整篇原文**（⛔ 不硬造分段）
PASS 17k ⑤ 相｜机械兜底（compaction/prune 区间）⇒ 条目数 = 1
PASS 17l ⑥ 反证｜同时传 summary 与 summaries ⇒ COLLECT_INVALID（planCollect 与 validatePlanInput 两处都拒）
PASS 17m ⑥ ★反证：挖掉守卫 ⇒ 不再报错、**静默挑一个**（summaries 赢了，summary 被丢）
```

**"旧写法下会怎样"（改造前后的真实读数，都是跑出来的）**：

```
· 17 改造后（3 段结构化，真链路）：
  {"entries":[["mt-0000-0001-1.md",563,["Serious"]],["mt-0000-0001-2.md",563,["Romantic"]],["mt-0000-0001-3.md",564,["Suspense"]]],
   "bodiesBytes":[1681,1681,1684]}
· 17 三段散文合计 1690 字；拼成一条时那一条 = 1692 字（含两个换行）
· 17 改造前（副本 A：拼成一条）：
  {"entries":[["mt-0000-0001.md",1692,["Serious","Romantic","Suspense"]]],"bodiesBytes":[5048]}；archived=1
· 17 反证（副本 B：tags 并集）：
  [["Serious","Romantic","Suspense"],["Serious","Romantic","Suspense"],["Serious","Romantic","Suspense"]]
· 17 反证（副本 C：现取计划丢段）：
  {"entries":[["mt-0000-0001.md",1692,["Serious","Romantic","Suspense"]]],"bodiesBytes":[5048]}；archived=1
· 17 反证（副本 D：守卫挖掉）⇒ {"willWrite":5,"summaries":2}
· 18 改造前（副本：区间级判据）⇒ selectModelSummary.kind=mechanical（model-summary-too-big）；
  区间摘要 kind=mechanical/model-summary-too-big；segments=无
```

**五个反证副本**（每条锚点唯一、改完立刻自证"确实改了"、跑完即删，⛔ 本仓 lib 一字未动，17v 锁死）：

| 副本 | 改回哪一处旧写法 | 必红的是 |
|---|---|---|
| A | `planScan` 提交处 + `replanForRegion` 都改回 `summary:`（一次一份 = 拼成一条） | 17a/17b（① 的判据） |
| B | `tags: s.tags` → `tags: sel.tags`（一段一份时的并集） | 17e（③ 的判据） |
| C | `replanForRegion` 只带 `region.summary`（计划看着对、落库少写） | 17a/17b |
| D | 挖掉 `if (isPlainObject(input.summary)) bad(SUMMARIES_BOTH_MSG)` | 17l（⑥ 的判据） |
| E | `segments.every((s) => s.tooBig)` 换回 `Buffer.byteLength(beauty.text) > …`（区间级） | 18a/18b |

**第 4 对里"老路逐字节不变"的独立证据**（一次性脚本，对照物 = `git show HEAD:lib/collect.js`）：

```
$ node _tmp-bytecheck-collect.mjs          # 跑完即删，⛔ 没给仓库留垃圾
SAME  plan 键序
SAME  plan 全文（含 hash / expectedRevisions / willWrite）
SAME  apply 的 written 清单
SAME  假 Tavern 盘上每个文件的**逐字内容**
--- plan.hash 对照 ---
SAME  hash
```
（同一份夹具：3 楼 + 一份带 `meta` 的摘要；对照的是 `planCollect` 的**整份计划**与 `applyCollect` 之后
**盘上每个文件的字面内容**。永久的那一半证据是 A1 台子的 **A14**：老路 plan 的键集合被冻结，多/少一个键就红。）

**第 6 对的两处守卫**（同一句话，两个入口）：

```
$ MUTATE=9 node _selftest-collect.mjs
RED  A16 ★ 两路并存（summary + summaries）⇒ COLLECT_INVALID … ｜ planCollect 应拒
反证成立：两路并存（summary + summaries）的守卫被挖掉 ⇒ 静默挑一个 ⇒ 断言 A16 变红
```

---

## 3 真机形状夹具（3 段 + `shadowedSeqs`）跑完整 `planScan`/`applyScan`

第 17 节的 `runSl()` 走的就是**完整链路**（`collectOnce(auto:true)` ⇒ `planScan` + `applyScan` +
真 A1 + 假 Tavern），夹具按任务书 §6-3 造：**一次 `compaction/summary` 的 `data.summary` 是一份
3 段的结构化 JSON**（每段一行「第N天 时段: …」散文 + 自己的 tags），三段散文合计 **1690 字**
（对齐真机 `mt-0000-0167.md` 那条 1690 字的读数），区间由一个带 `shadowedSeqs` 的替换事件定义。

| | 条目数 | 每条的正文 | 每条的 tags |
|---|---|---|---|
| **改造前**（副本 A：一次一份 = 拼成一条） | **1** 条（`mt-0000-0001.md`，**1692 字**） | 三段**挤在一条**里（`甲\n乙\n丙`，带两个换行） | **并集** `[Serious, Romantic, Suspense]` |
| **改造后**（本仓 lib） | **3** 条（`mt-0000-0001-1/2/3.md`） | 各是**那一段**的散文（563 / 563 / 564 字，合计 1690） | **段级** `[Serious]` / `[Romantic]` / `[Suspense]` |

- **楼层只写一遍**：**17g/17h** 用假 Tavern 的**请求流水**证明 —— 2 个楼层路径各 PUT 一次、3 个摘要路径
  各 PUT 一次，没有"写 N 遍同一份"，也没有任何"重复"warning。
- **检索侧的意义**（改造后每条 = 一个向量点）：真机那 45 段以后就是 45 条切片（派单方重收时核），
  不再是"一个点 = 1690 字整篇"。

---

## 4 「没做 / 不确定」（⛔ 不替派单方下结论）

1. **真机重收后的条目数与内容 —— 要等部署之后核**。任务是「重收后应变成 **46 条**左右、每条一段」；
   我这里能保证的是**链路形状**（N 段 ⇒ N 条 entry、每条一段、tags 段级），**真机那份摘要到底几段、
   每段多少字，我读不到**（§1 的读数是派单方给的）⇒ 我不下结论。真机读数请核：`index.json` 的
   `entries.length`、`manifest.summaries.count`、以及 `summaries/` 下的 `.md` 个数是否相等。
2. **夹具是"同长"不是"同一份"**：三段散文合计 1690 字是我按真机读数**对齐长度**造的合成占位散文
   （⛔ 无任何真实会话/提示词正文），不是真机那份的内容；真机那份的段数与分段前缀以真机为准。
3. **段与楼层没有一一对应**：模型的分段跟楼层不是一一对应关系 ⇒ 各段**共用**区间的 `fromFloor/toFloor`
   （`index.json` 里 N 条的 from/to 相同）。这是任务书 §2.2 的明确口径（「如实共用，⛔ 别硬编」）。
4. **区间级的 tags 并集仍然在**（`plan.regions[].summary.meta.tags`）：它是**区间视图 / 老读者**用的，
   **不是**落盘的那份 —— 落盘的每段 tags 在各自的 entry 里。若派单方要求把区间视图也去掉，那是另一条改动。
5. **某一段没有 tags 对象时不额外播报**：那一段的 entry **没有 `tags` 字段**（A1 白名单：`meta` 为 `null`
   就不并入），播报仍按**并集**判（沿用 `tags-empty` / `tags-partial` 的现枚举与现文案，⛔ 没新增码）。
   若派单方要"逐段播报 tags-empty"，那是另一条改动（我按"枚举不变"的口径选了不动）。
6. **两段散文完全一样时不按内容去重**：两条 entry 各自一个文件（id 不同），如实按段落落盘；
   检索侧会拿到两份一样文本的切片 —— 我认为这比"静默合并"更符合"如实"，但没主动加去重。
7. **面板（`lib/client.js`）没加"这条摘要切成了几段"的显示**：任务书没要求，且端点的对外形状与状态码
   一个字没动（`publicRegionOf` 未改）⇒ 面板看到的还是老字段。若要在面板上显示段数，是另一条改动。
8. **`lib/collect.js` 是本单唯一允许改的既有文件，我只做了追加**：新分支都由 `input.summaries` 触发，
   老路走的还是同一段代码；一次性对照脚本那五项 SAME 是我能给出的最强证据（永久版是 A14 的形状冻结）。
9. **上一单/上上单的 report 我没删**：`report-20260922-161203-收纳区间一次压缩一段.md`（上上单）与
   `report-20260922-163935-摘要美化+tags标签化.md`（上一单，本轮由 `report.md` 改名保留）都在；
   新的 `report.md` 就是本文件。⛔ **未 commit / 未 push / 未部署**，也**没碰**任何禁区目录。

---

## 5 CHANGELOG

`CHANGELOG.md` 的 `[Unreleased]` **顶部**补了一条
「2026-09-22（归档摘要**按段切片**：一条摘要拆成「一个叙事段一条」—— 检索的粒度就是「一条文件」）」
（**⛔ 没有起新版本号**：`package.json` 仍是 `0.7.0`，我一个字没动它）。

---

## 6 复核用的命令（我跑过的原样）

```bash
node --check lib/collect.js                                        # OK
node --check lib/collect-scan.js                                   # OK
node --check lib/collect-scan.js lib/collect.js                    # OK（退出码 0）
node _selftest-collect-scan.mjs                                    # 155 通过 / 0 失败（第 17/18 节是本单新增）
node _selftest-collect.mjs                                         # 17/17 绿（A14–A18 是本单新增）
MUTATE=9 node _selftest-collect.mjs                                # 反证成立：A16 变红（退出码 0）
node /d/apps/dsh-tar*配置区/产物/memory-tools/_run-all-selftests.mjs  # === 67 passed / 0 failed ===（exit=0）
git diff --stat                                                    # 本单 4 个文件 + 上两单未提交的 3 个
git diff --stat -- lib/index.js lib/summarize.js _selftest-summarize.mjs   # 39/16/39 —— 与开工时逐字相同
```
